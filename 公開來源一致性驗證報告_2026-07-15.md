# 成交聯盟公開來源一致性驗證報告

日期：2026-07-15  
作品 ID：`DA-WEB-ORIGIN-MISMATCH-GATE-20260715-V2`  
分類：內部發布驗證器與唯讀驗證紀錄  
狀態：`BLOCKED_PUBLIC_ORIGIN_MISMATCH`

## 範圍與安全邊界

本輪只修改下列既有 GET-only 驗證器、其 loopback fixture 測試，以及本報告；未修改 `dist/`、網站來源、DNS、custom domain、Pages、Cloudflare 或任何外部設定。

- `tests/verify_public_origin_mismatch.py`
- `tests/test_verify_public_origin_mismatch.py`

目前受控部署的預設 preview 固定為 Cloudflare Pages：`https://a94e57a5.deal-alliance-official-site.pages.dev`。正式 base URL 是 `https://www.dealalliancehub.com`。預設退役路徑是 `/tools/life-number-calculator/`；可重複給 `--retired-path` 加入其他已明確核准的退役路徑。

請求只會使用明確 `GET`、無 request body、無 cookie jar、無認證、無 secret、無登入，而且不跟隨 redirect。production 模式只接受 HTTPS；`--allow-http-fixtures` 僅允許 `127.0.0.1`／`localhost` 的本地 fixture。

## 判定規則

每個退役路徑必須同時符合：

1. Pages preview 與正式公開站 HTTP status 完全相同。
2. 兩邊皆為 `404`。
3. HTML `<title>` 完全相同。
4. 回應 body 的 SHA-256 content fingerprint 完全相同。

任一項不符即輸出 `BLOCKED_PUBLIC_ORIGIN_MISMATCH`，exit code 為 `2`；不可把候選本地 Gate PASS 說成正式公開站已同步。

## 端到端驗證

本機 loopback fixture（去識別化，暫時 `127.0.0.1` HTTP server）已通過 6/6：

```text
python3 -B tests/test_verify_public_origin_mismatch.py
Ran 6 tests ... OK
```

涵蓋 CLI 預設值必為目前 Cloudflare Pages preview、同一退役 404 的 PASS、HTTP status 差異、title 差異、內容指紋差異，以及 production 模式拒絕 HTTP。fixture 也確認兩個 request 都是 `GET`；未使用真實資料。

## Legacy GitHub Pages 與本次 Pages preview 的分界

`https://orikan347.github.io/deal-alliance-official-site` 是先前的 GitHub Pages 備援／歷史部署 URL，不是本次受控 Cloudflare Pages deployment preview。先前任何以該 URL 得到的 readback 只保留為歷史紀錄，**不可**用於判定本次部署；驗證器的 default 已不再指向它。

若有必要回查 legacy URL，必須明確傳入 `--preview-origin https://orikan347.github.io/deal-alliance-official-site`，並在報告中標為 legacy，不可覆蓋本次 Cloudflare Pages 結論。

## 唯讀公開結果

實際執行（無憑證、GET-only；使用 default Cloudflare Pages preview）：

```text
python3 -B tests/verify_public_origin_mismatch.py --timeout 15
BLOCKED_PUBLIC_ORIGIN_MISMATCH path=/tools/life-number-calculator/ check=http_status_mismatch preview=404 public=200
BLOCKED_PUBLIC_ORIGIN_MISMATCH path=/tools/life-number-calculator/ check=retired_path_not_404 preview=404 public=200
BLOCKED_PUBLIC_ORIGIN_MISMATCH path=/tools/life-number-calculator/ check=title_mismatch preview='找不到這個公開頁面｜成交聯盟' public='生涯運數計算器｜成交聯盟'
BLOCKED_PUBLIC_ORIGIN_MISMATCH path=/tools/life-number-calculator/ check=content_fingerprint_mismatch preview_sha256=5c51712053338e953e30f64f4bed075857950109df9e3febc9ee18a5d769fb36 public_sha256=ace8c1e9b1cb41c9fe34b4a1cf4c99283a264db3ded15579c21e224bcdbf6a0d
```

結論：截至本次 readback，本次 Cloudflare Pages preview 已正確將退役路徑回應為 `404`；正式公開站仍回應 `200`、保留舊 title，且 body fingerprint 不同。正式公開來源尚未收斂成這次受控部署的退役 `404` 狀態；本輪應維持封鎖，不得宣稱此退役變更已正式發布。

## 追溯資訊

| source_path | SHA-256 | verification_status |
|---|---|---|
| `tests/verify_public_origin_mismatch.py` | `4032f9f962ab1dda94a65c4cb6fd94cdc392f154bbb60f618519dd58412f8ab5` | fixture 6/6 PASS；current Pages readback correctly BLOCKED |
| `tests/test_verify_public_origin_mismatch.py` | `ae1b4d36264f0e5e9b5dbe84424979361eff99e200f8c47777d8c119067f6a1a` | fixture 6/6 PASS；CLI default verified |

publication_or_delivery_record：未發布、未部署；僅寫入本候選資料夾的內部驗證紀錄。

## 風險與回滾

目前風險是正式公開站的退役路徑仍可達（`200`），而 current Pages preview 已是 `404`。修正正式來源後，應重新讀回兩端皆為 `404`、title 相同、fingerprint 相同，才可解除 gate；本報告不授權執行該修正。

本輪沒有外部變更，因此沒有外部回滾動作。若要撤回這次 verifier，應在擁有此候選資料夾的正式 Git 工作樹中只還原本報告與兩個 `origin_mismatch` 新檔；不要回滾或覆寫其他人的候選、`dist/` 或部署設定。

## Git 提交狀態

`200_官網整合/公開官網候選_v1/` 與其父工作目錄目前沒有 `.git` metadata；在提供的 workspace 中也沒有此官方候選站的 Git 工作樹。因此未初始化新 repo、未猜測 remote、未建立空白 commit。提交狀態：`PENDING_COMMIT_NO_GIT_WORKTREE`。
