# 成交聯盟官方網站候選 v1｜專屬驗收報告

作品 ID：`ORI-WEB-DA-OFFICIAL-MVP-20260713-V1`  
目前階段：`PENDING_USER_ACCEPTANCE`  
狀態：`FUNCTIONAL_TEST_READY_NOT_DEPLOYED`  
範圍：僅 `200_官網整合/公開官網候選_v1/`

本輪更新：主窗口已確認正式來源為 `https://www.dealalliancehub.com`。本機 `site.config.json`、全站 canonical、OG、JSON-LD、`sitemap.xml`、`robots.txt` 與 `llms.txt` 已統一切換；尚未 push、部署、修改 DNS／Cloudflare 或接收真實資料。

## 已驗證

- 公開頁已補齊首頁、about、solutions、tools、六項工具詳情、resources、FAQ、候補、privacy、terms、404 與搜尋空狀態。
- 每個公開索引頁具唯一 title、description、canonical、OG 欄位、手機 viewport、單一 H1 與可解析 JSON-LD。
- sitemap 包含 15 個公開頁；404 與搜尋空狀態不進 sitemap。
- 候補互動預設為本機假資料示意；沒有 form action、XMLHttpRequest、sendBeacon 或外部資料寫入，遠端 `fetch` 只有在明確啟用且通過端點白名單時才會執行。
- 候補收件安全契約已建立：runtime 預設 `disabled`；正式啟用前會拒絕非 HTTPS／未列入 Origin 白名單的端點，要求同意，送出最小欄位，並只接受 HTTP 202＋`request_id`＋`status=received` readback。
- 假收件 E2E：`PASS_WAITLIST_FAKE_E2E status=202 readback=request_id dedupe=stable reject=400 fake_payload_only default_mode=disabled`；fake receiver 僅使用 `example.invalid` 測試資料，且只存在記憶體內。
- robots 保留公開頁可抓取，並明確列出 `/login/`、`/admin/`、`/students/`、`/private/`、`/drafts/` 邊界。
- 工具目錄共六項；正式服務仍只提供候補／洽詢。四個工具頁另有本機假資料示範，但不宣稱已上線、不下載正式工具、不付款、不登入、不呼叫正式 API、不保存或發送。
- 完整網站規劃藍圖已存在，並由驗收腳本確認 8 個必要規劃區塊。
- 首頁已完成成交聯盟自己的品牌視覺系統：成交藍／聯盟藍／深鈷藍／系統霧白、工具箱＋勾線＋星芒 Logo、問題入口、方法流程、工具狀態與開放通知形成完整首頁敘事。雷蒙研究僅用於資訊架構與轉換路徑，不使用其外觀。
- 品牌資產驗證：所有公開頁共用官方色彩變數與 `assets/brand-mark.svg`；未沿用研究網站的 Logo、配色或裝飾。
- 已建立獨立的 `公開推播前交接清單_2026-07-13.md`，列出正式網域、內容核准、staging SEO/GEO readback、發布授權、禁止動作與回滾方式。
- 跨站 SEO／GEO Gate：`PASS_LOCAL_PREPARATION_PENDING_EXTERNAL`，`error_count=0`；外部驗證仍為 `PENDING`。
- 靜態安全標頭候選：`_headers` 已加入 `nosniff`、嚴格 referrer、權限限制、CSP、同源連線、同源表單與禁止 frame；正式 host 仍需實際 response header readback。
- 正式發布契約：`release_contract.json` 定義 19 個公開路徑、候補 202/readback、監控項目、敏感資料記錄限制與回滾觸發條件；目前明確標示 `PENDING_EXTERNAL_RELEASE`。
- 公開發布後只讀 Gate 已新增：`tests/verify_public_release.py` 會檢查正式 HTTPS、根網域轉 www、19 個公開路徑、安全標頭、canonical、OG、JSON-LD、sitemap 與 crawler 資產；目前因正式網域 DNS 未解析，尚未取得公開 PASS。
- 工具狀態對照：`PASS_TOOL_SOURCE_ALIGNMENT products=5 source=400_status_table`；網站目錄會檢查簡訊、LINE、通訊錄轉換、Smart Close、生涯運數的目前狀態標記，避免公開文案脫離工具主線真相。
- 發布 artifact 邊界：Pages workflow 只會把公開 HTML、assets、robots、sitemap、`llms.txt` 與 `_headers` 複製到 `dist/`，排除測試、交接文件、候選設定與契約 JSON；目前僅完成候選流程，未觸發部署。
- 正式來源切換 Gate：`PASS_RELEASE_CONTRACT origin=https://www.dealalliancehub.com origin_status=OWNER_CONFIRMED_NOT_DEPLOYED`；本機 canonical／OG／JSON-LD／sitemap／`llms.txt` 已完成一致性檢查。
- 正式網域只讀檢查（2026-07-13）：`www.dealalliancehub.com` 與根網域 DNS 均無法解析；因此正式 HTTPS、公開路由、crawler readback 與候補 receiver 尚未驗證。
- 本輪 UX 修補：補上全站 `.button-secondary` 次要按鈕的品牌樣式與 hover 狀態，並加入 `prefers-reduced-motion` 支援，降低動態效果對使用者的干擾。
- 404 可用性修補：`404.html` 改為完整共用網站 shell，含跳過導覽、主導覽、麵包屑、清楚的錯誤說明、回首頁／工具入口與頁尾，不再是孤立的單行錯誤頁。
- 手機導覽修補：390px 寬度下保留完整主導覽，改為可橫向滑動的導覽列，不再把「解決方案／工具／公開資源／FAQ」隱藏；CTA 維持可見且不被擠壞。

## 功能測試證據（Codex 假資料）

- 聯絡人轉換成功：輸入兩筆 `example.invalid` 假名單，產生 `2 筆 VCF 預覽`，下載按鈕只在本機出現，沒有上傳或保存。
- 聯絡人轉換錯誤：清空輸入後執行，顯示 `請輸入假資料，每行格式：姓名,電話,email`，不產生下載。
- 聯絡人轉換格式錯誤：輸入欄位不足或 Email 格式不正確時，顯示第幾行錯誤並清除下載狀態。
- Smart Close：輸入「王測試：客戶說預算需要再討論，請下週回覆。」後產生本機整理結果，明確顯示未呼叫 AI、未保存內容。
- 簡訊工具：輸入假收件人與內容後產生預覽，明確顯示沒有發送。
- LINE 工具：輸入讀取、篩選、預覽、取消四步驟後產生流程預覽，明確顯示沒有登入 LINE 或發送訊息。
- 候補頁：使用 `visitor@example.invalid` 與勾選同意，在本機瀏覽器實際操作候選頁；頁面顯示未送出資料、未建立候補名單。未勾選同意時由必要核取方塊阻止送出。正式 receiver 未連接。
- 本輪瀏覽器 readback：404 頁可見完整導覽、麵包屑、404 標題、回首頁／工具 CTA 與頁尾；About 與聯絡人轉換頁的次要 CTA 讀到白色背景與品牌邊框。候補頁以假 Email 測試時，未勾選同意由瀏覽器 required 防線阻止；勾選後只顯示本機未送出訊息。
- 手機瀏覽器 readback：以 390×844 viewport 開啟首頁，`.nav-links` 計算樣式為 `display:flex`、`overflow-x:auto`、導覽內容寬 354px，CTA 寬 131px；完整導覽節點仍可見。
- 取消流程：四個本機示範與候補表單都會動態提供「清除／取消」控制，清除輸入、預覽與本機狀態，不呼叫外部服務。

以上是 Codex 的功能測試證據；目前仍需要使用者在本機逐項操作並確認可用，不能把這份證據當成使用者驗收或正式發布。

## 執行方式

```bash
python3 tests/verify_site.py
```

測試會檢查 metadata/schema、公開邊界、sitemap、手機 CSS、所有公開頁的本機連結、4 條假訪客路徑、六項工具狀態、四個本機示範標記、錯誤／取消控制、禁止承諾詞、完整規劃藍圖、404／搜尋空狀態，以及以 Python 靜態 HTTP server 讀取公開路由。

本次重跑結果：`PASS_RELEASE_CONTRACT paths=19 monitoring=6 external_pending=true`、`PASS_HEADERS_CONTRACT security_headers=7 candidate_only=true`、`PASS_WAITLIST_FAKE_E2E status=202 readback=request_id dedupe=stable reject=400 fake_payload_only default_mode=disabled`、`PASS_METADATA_AND_SCHEMA pages=15`、`PASS_PUBLIC_BOUNDARY local_form_no_submit=true default_endpoint_disabled=true tool_catalog=6`、`PASS_TOOL_SOURCE_ALIGNMENT products=5 source=400_status_table`、`PASS_SITEMAP_AND_MOBILE sitemap_urls=15`、`PASS_FAKE_VISITOR_E2E journeys=4 fake_payload=not_persisted local_form=simulated_only`、`PASS_PUBLIC_CLAIM_SCAN forbidden_terms=0`、`PASS_PLANNING_CONTRACT sections=8 scope=official-site-only`、`PASS_LOCAL_HTTP routes=20`、`PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com deployment_pending=true`。另以本機瀏覽器實際 readback 404、About、聯絡人轉換與候補頁：404 共用 shell 正常渲染；次要 CTA 樣式可見；候補頁完成假 Email＋同意的本機示意、未勾選同意時不送出。另以 390×844 手機 viewport 驗證首頁主導覽可見且可橫向滑動。沒有外部送出或保存。

## 追溯摘要

- `index.html`：`22d217244a363d3b3514d0a2d3e1c9beea33c08fa76985691657bbd365db6cdb`
- `assets/site.js`：`55786196b2c909e28a209f8b8a3be07ad4cd05175fd470998520a23afec8eca1`
- `assets/styles.css`：`3ec960441fdfcaebdf050c6bcfff03383f91abd9ce10547d27d1b176ac203ebc`
- `tests/verify_site.py`：`fd42efc58fb4690db07d484ae976bd2c0b2f692b41a9e11cf69709bb029b5ce5`
- `404.html`：`b0b58342bc04e72f46d4a4d1c2cebaedf8724dc0071a2595e1c10e6de2343cfc`
- `assets/brand-mark.svg`：`8880202f2e53add602d0d40308c14b03f3d53e9d567b7dca99d246cc7abd90e3`
- `about/index.html`：`6c2306423d85996a9d66a86fdf4124d38d04ae5aae85e26196a1e621025ca23b`
- `terms/index.html`：`f03f01de72a2f76271b6087de2468bab7ed9cc6aca7dce0be0c07ff767238fc0`
- `sitemap.xml`：`e4a7a6e8feedd392de48d3bcf40945e6372397d0a865bf32bdca8e027345b2da`
- `robots.txt`：`ee015512e076197714977b9db04a44595e5cb9cd1dd1578a1a1b4e56d7c49636`
- `tests/verify_site.py`：`107b9f6ab291724f10e9330b375c29f9d41dc775d987e079f84e1c5caa4413d0`
- `tests/verify_public_release.py`：`dab6288cd0782c33162a7ce97ca14f236c9196a0ebd1d676a7002a149097a226`
- `網站完整規劃藍圖_2026-07-13.md`：`0a763173e84d3d943bb26e6a60cf4a7adef134f6060ce9fd68b0fc4c4b659e2e`
- `README.md`：`704c200b9105712d75208304029c1ea1b2a841887ad0531e403131882e0e3121`
- `公開推播前交接清單_2026-07-13.md`：`2e3ac879224c6d66926a46d44f0952889729a8e887bc414931fda9adbc4d0d3f`

## 未完成風險

1. 使用者尚未完成核心流程驗收；目前不得宣稱正式完成。
2. 正式來源已確認為 `https://www.dealalliancehub.com`；尚未部署，不能宣稱公開 HTTPS 已可用。
3. 尚未執行正式 HTTPS、公開 crawler、分享卡 unfurl、Schema Validator、Rich Results Test、Search Console 或 Bing readback。
4. 公開品牌文字、工具事實、公開資源、候補欄位、正式隱私與條款仍待主窗口／品牌／法務核准。
5. 候補 receiver 尚未提供正式 HTTPS URL、Origin 設定、保存／刪除政策與 operator readback；目前只能跑 fake receiver。
6. SEO／GEO Gate 的 21 項外部檢查（正式網域、crawler、Search Console、Bing 等）尚未執行；本機準備 Gate 已通過。

## 外部動作紀錄

- 已依使用者明確要求，將本候選版推送至 GitHub `Orikan347/deal-alliance-official-site` 的 `agent/official-site-v1` 分支。
- 推送 commit：`5637fb7d30129a32aabb8f249b59c60dde6e9d46`（`Prepare Deal Alliance official site candidate v1`）。
- 本次功能示範與驗收文件更新尚未另行推送；目前只存在本機候選資料夾。
- GitHub 只讀查核確認遠端 `agent/official-site-v1` 仍落後於本機候選，且沒有正式網域／Pages 設定證據；本輪沒有 push。
- GitHub Pages 標準網址只讀探測回傳 HTTP `404`；沒有公開 URL 可供正式 SEO/GEO 或候補 readback 驗收。
- 未部署至公開網址、未送出表單、未建立外部帳號、未讀取真實客戶／學生資料，亦未修改 Cloudflare、付款、secrets、授權或 `l2_current_truth.json`。

## 回滾

本輪檔案均位於候選資料夾。若需撤回，可還原本次變更檔案或回到上一個候選版本；若日後另行推送，使用 Git revert 撤回候選 commit。不影響正式網站、Cloudflare、後台、付款、授權或共同真相。

## 2026-07-13 正式公開驗收結果（以本節為最新狀態）

- 正式公開網址：`https://www.dealalliancehub.com`。
- Cloudflare Pages 預設網址：`https://deal-alliance-official-site.pages.dev`，已回應 HTTPS `200`。
- 根網址：`https://dealalliancehub.com` 已以 `301` 導向正式 `www` 網址。
- 公開 DNS：Cloudflare 公開解析器已回傳 `www.dealalliancehub.com` 的 edge IP；正式 HTTPS 首頁實測 `200`，並讀到 `Content-Security-Policy`、`X-Content-Type-Options`、`Referrer-Policy`。
- 原始正式驗收器已實測通過：`PASS_PUBLIC_RELEASE origin=https://www.dealalliancehub.com paths=19 root_redirect=www crawler_assets=ok waitlist_post=not_performed resolver=public_doh+system_dns`。
- 為避免本機 Tailscale／系統 DNS 短暫負向快取造成假失敗，`tests/verify_public_release.py` 已改為系統 DNS 失敗時改用公開 DNS-over-HTTPS；它仍只做 GET、仍不提交候補表單，也不讀取任何私人端點。
- 候補表單仍維持 disabled：沒有 receiver、沒有真實名單、沒有登入／付款／下載啟用宣稱。

以上證據只確認「公開官網」已正式可用；學生後台、付款、工具授權、下載、App 簽章與真實候補 receiver 仍是其他工作線，未因官網發布而自動啟用。
