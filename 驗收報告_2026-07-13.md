# 成交聯盟官方網站候選 v1｜專屬驗收報告

作品 ID：`ORI-WEB-DA-OFFICIAL-MVP-20260713-V1`
目前階段：`PENDING_RELEASE_SECURITY`
狀態：`PUBLIC_RELEASE_VERIFIED_PENDING_REGISTRATION`
範圍：僅 `200_官網整合/公開官網候選_v1/`

本輪更新：本機候選的 `site.config.json`、全站 canonical、OG、JSON-LD、`sitemap.xml`、`robots.txt` 與 `llms.txt` 已統一切換。註冊／登入仍未接後台，也未接收真實資料。正式網址曾通過基礎公開 readback；但本報告最末的「最新公開候選同步讀回」才是目前部署同步狀態的唯一依據。

## 已驗證

- 公開頁已補齊首頁、about、solutions、tools、六項工具詳情、resources、FAQ、候補、privacy、terms、404 與搜尋空狀態。
- 每個公開索引頁具唯一 title、description、canonical、OG 欄位、手機 viewport、單一 H1 與可解析 JSON-LD。
- sitemap 包含 15 個公開頁；404 與搜尋空狀態不進 sitemap。
- 候補互動預設為本機假資料示意；沒有 form action、XMLHttpRequest、sendBeacon 或外部資料寫入，遠端 `fetch` 只有在明確啟用且通過端點白名單時才會執行。
- 候補收件安全契約已建立：runtime 預設 `disabled`；正式啟用前會拒絕非 HTTPS／未列入 Origin 白名單的端點，要求同意，送出最小欄位，並只接受 HTTP 202＋`request_id`＋`status=received` readback。
- 假收件 E2E：`PASS_WAITLIST_FAKE_E2E status=202 readback=request_id dedupe=stable reject=400 fake_payload_only default_mode=disabled`；fake receiver 僅使用 `example.invalid` 測試資料，且只存在記憶體內。
- robots 保留公開頁可抓取，並明確列出 `/login/`、`/admin/`、`/students/`、`/private/`、`/drafts/` 邊界。
- 工具目錄共六項；正式服務仍只提供候補／洽詢。四個工具頁另有本機假資料示範，但不宣稱已上線、不下載正式工具、不付款、不登入、不呼叫正式 API、不保存或發送。
- 完整網站規劃藍圖已存在，並由驗收腳本確認 8 個必要規劃區塊。
- 首頁已完成成交聯盟自己的品牌視覺系統：成交藍／聯盟藍／深鈷藍／系統霧白、工具箱＋勾線＋星芒 Logo、問題入口、方法流程、工具狀態與開放通知形成完整首頁敘事。雷蒙研究僅用於資訊架構與轉換路徑，不使用其外觀。
- 品牌資產驗證：所有公開頁共用官方色彩變數與 `assets/brand-mark.svg`；未沿用研究網站的 Logo、配色或裝飾。
- 已建立獨立的 `公開推播前交接清單_2026-07-13.md`，列出正式網域、內容核准、staging SEO/GEO readback、發布授權、禁止動作與回滾方式。
- 跨站 SEO／GEO Gate：`PASS_LOCAL_PREPARATION_PENDING_EXTERNAL`，`error_count=0`；外部驗證仍為 `PENDING`。
- 靜態安全標頭候選：`_headers` 已加入 `nosniff`、嚴格 referrer、權限限制、CSP、同源連線、同源表單與禁止 frame；正式 host 仍需實際 response header readback。
- 正式發布契約：`release_contract.json` 定義 19 個公開路徑、候補 202/readback、監控項目、敏感資料記錄限制與回滾觸發條件；目前明確標示 `PENDING_EXTERNAL_RELEASE`。
- 公開發布後只讀 Gate 已新增：`tests/verify_public_release.py` 會檢查正式 HTTPS、根網域轉 www、19 個公開路徑、安全標頭、canonical、OG、JSON-LD、sitemap 與 crawler 資產；目前因正式網域 DNS 未解析，尚未取得公開 PASS。
- 工具狀態對照：`PASS_TOOL_SOURCE_ALIGNMENT products=5 source=400_status_table`；網站目錄會檢查簡訊、LINE、通訊錄轉換、Smart Close、生涯運數的目前狀態標記，避免公開文案脫離工具主線真相。
- 發布 artifact 邊界：Pages workflow 只會把公開 HTML、assets、robots、sitemap、`llms.txt` 與 `_headers` 複製到 `dist/`，排除測試、交接文件、候選設定與契約 JSON；目前僅完成候選流程，未觸發部署。
- 正式來源切換 Gate：`PASS_RELEASE_CONTRACT origin=https://www.dealalliancehub.com origin_status=OWNER_CONFIRMED_NOT_DEPLOYED`；本機 canonical／OG／JSON-LD／sitemap／`llms.txt` 已完成一致性檢查。
- 正式網域只讀檢查（2026-07-13）：`www.dealalliancehub.com` 與根網域 DNS 均無法解析；因此正式 HTTPS、公開路由、crawler readback 與候補 receiver 尚未驗證。
- 本輪 UX 修補：補上全站 `.button-secondary` 次要按鈕的品牌樣式與 hover 狀態，並加入 `prefers-reduced-motion` 支援，降低動態效果對使用者的干擾。
- 404 可用性修補：`404.html` 改為完整共用網站 shell，含跳過導覽、主導覽、麵包屑、清楚的錯誤說明、回首頁／工具入口與頁尾，不再是孤立的單行錯誤頁。
- 手機導覽修補：390px 寬度下保留完整主導覽，改為可橫向滑動的導覽列，不再把「解決方案／工具／公開資源／FAQ」隱藏；CTA 維持可見且不被擠壞。

## 功能測試證據（Codex 假資料）

- 聯絡人轉換成功：輸入兩筆 `example.invalid` 假名單，產生 `2 筆 VCF 預覽`，下載按鈕只在本機出現，沒有上傳或保存。
- 聯絡人轉換錯誤：清空輸入後執行，顯示 `請輸入假資料，每行格式：姓名,電話,email`，不產生下載。
- 聯絡人轉換格式錯誤：輸入欄位不足或 Email 格式不正確時，顯示第幾行錯誤並清除下載狀態。
- Smart Close：輸入「王測試：客戶說預算需要再討論，請下週回覆。」後產生本機整理結果，明確顯示未呼叫 AI、未保存內容。
- 簡訊工具：輸入假收件人與內容後產生預覽，明確顯示沒有發送。
- LINE 工具：輸入讀取、篩選、預覽、取消四步驟後產生流程預覽，明確顯示沒有登入 LINE 或發送訊息。
- 候補頁：使用 `visitor@example.invalid` 與勾選同意，在本機瀏覽器實際操作候選頁；頁面顯示未送出資料、未建立候補名單。未勾選同意時由必要核取方塊阻止送出。正式 receiver 未連接。
- 本輪瀏覽器 readback：404 頁可見完整導覽、麵包屑、404 標題、回首頁／工具 CTA 與頁尾；About 與聯絡人轉換頁的次要 CTA 讀到白色背景與品牌邊框。候補頁以假 Email 測試時，未勾選同意由瀏覽器 required 防線阻止；勾選後只顯示本機未送出訊息。
- 手機瀏覽器 readback：以 390×844 viewport 開啟首頁，`.nav-links` 計算樣式為 `display:flex`、`overflow-x:auto`、導覽內容寬 354px，CTA 寬 131px；完整導覽節點仍可見。
- 取消流程：四個本機示範與候補表單都會動態提供「清除／取消」控制，清除輸入、預覽與本機狀態，不呼叫外部服務。

以上是 Codex 的功能測試證據；目前仍需要使用者在本機逐項操作並確認可用，不能把這份證據當成使用者驗收或正式發布。

## 執行方式

```bash
python3 tests/verify_site.py
```

測試會檢查 metadata/schema、公開邊界、sitemap、手機 CSS、所有公開頁的本機連結、4 條假訪客路徑、六項工具狀態、四個本機示範標記、錯誤／取消控制、禁止承諾詞、完整規劃藍圖、404／搜尋空狀態，以及以 Python 靜態 HTTP server 讀取公開路由。

本次重跑結果：`PASS_RELEASE_CONTRACT paths=19 monitoring=6 external_pending=true`、`PASS_HEADERS_CONTRACT security_headers=7 candidate_only=true`、`PASS_WAITLIST_FAKE_E2E status=202 readback=request_id dedupe=stable reject=400 fake_payload_only default_mode=disabled`、`PASS_METADATA_AND_SCHEMA pages=15`、`PASS_PUBLIC_BOUNDARY local_form_no_submit=true default_endpoint_disabled=true tool_catalog=6`、`PASS_TOOL_SOURCE_ALIGNMENT products=5 source=400_status_table`、`PASS_SITEMAP_AND_MOBILE sitemap_urls=15`、`PASS_FAKE_VISITOR_E2E journeys=4 fake_payload=not_persisted local_form=simulated_only`、`PASS_PUBLIC_CLAIM_SCAN forbidden_terms=0`、`PASS_PLANNING_CONTRACT sections=8 scope=official-site-only`、`PASS_LOCAL_HTTP routes=20`、`PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com deployment_pending=true`。另以本機瀏覽器實際 readback 404、About、聯絡人轉換與候補頁：404 共用 shell 正常渲染；次要 CTA 樣式可見；候補頁完成假 Email＋同意的本機示意、未勾選同意時不送出。另以 390×844 手機 viewport 驗證首頁主導覽可見且可橫向滑動。沒有外部送出或保存。

## 追溯摘要

- `index.html`：`22d217244a363d3b3514d0a2d3e1c9beea33c08fa76985691657bbd365db6cdb`
- `assets/site.js`：`55786196b2c909e28a209f8b8a3be07ad4cd05175fd470998520a23afec8eca1`
- `assets/styles.css`：`3ec960441fdfcaebdf050c6bcfff03383f91abd9ce10547d27d1b176ac203ebc`
- `tests/verify_site.py`：`fd42efc58fb4690db07d484ae976bd2c0b2f692b41a9e11cf69709bb029b5ce5`
- `404.html`：`b0b58342bc04e72f46d4a4d1c2cebaedf8724dc0071a2595e1c10e6de2343cfc`
- `assets/brand-mark.svg`：`8880202f2e53add602d0d40308c14b03f3d53e9d567b7dca99d246cc7abd90e3`
- `about/index.html`：`6c2306423d85996a9d66a86fdf4124d38d04ae5aae85e26196a1e621025ca23b`
- `terms/index.html`：`f03f01de72a2f76271b6087de2468bab7ed9cc6aca7dce0be0c07ff767238fc0`
- `sitemap.xml`：`e4a7a6e8feedd392de48d3bcf40945e6372397d0a865bf32bdca8e027345b2da`
- `robots.txt`：`ee015512e076197714977b9db04a44595e5cb9cd1dd1578a1a1b4e56d7c49636`
- `tests/verify_site.py`：`107b9f6ab291724f10e9330b375c29f9d41dc775d987e079f84e1c5caa4413d0`
- `tests/verify_public_release.py`：`dab6288cd0782c33162a7ce97ca14f236c9196a0ebd1d676a7002a149097a226`
- `網站完整規劃藍圖_2026-07-13.md`：`0a763173e84d3d943bb26e6a60cf4a7adef134f6060ce9fd68b0fc4c4b659e2e`
- `README.md`：`704c200b9105712d75208304029c1ea1b2a841887ad0531e403131882e0e3121`
- `公開推播前交接清單_2026-07-13.md`：`2e3ac879224c6d66926a46d44f0952889729a8e887bc414931fda9adbc4d0d3f`

## 未完成風險

1. 使用者尚未完成核心流程驗收；目前不得宣稱正式完成。
2. 正式來源已確認為 `https://www.dealalliancehub.com`；尚未部署，不能宣稱公開 HTTPS 已可用。
3. 尚未執行正式 HTTPS、公開 crawler、分享卡 unfurl、Schema Validator、Rich Results Test、Search Console 或 Bing readback。
4. 公開品牌文字、工具事實、公開資源、候補欄位、正式隱私與條款仍待主窗口／品牌／法務核准。
5. 候補 receiver 尚未提供正式 HTTPS URL、Origin 設定、保存／刪除政策與 operator readback；目前只能跑 fake receiver。
6. SEO／GEO Gate 的 21 項外部檢查（正式網域、crawler、Search Console、Bing 等）尚未執行；本機準備 Gate 已通過。

## 外部動作紀錄

- 已依使用者明確要求，將本候選版推送至 GitHub `Orikan347/deal-alliance-official-site` 的 `agent/official-site-v1` 分支。
- 推送 commit：`5637fb7d30129a32aabb8f249b59c60dde6e9d46`（`Prepare Deal Alliance official site candidate v1`）。
- 本次功能示範與驗收文件更新尚未另行推送；目前只存在本機候選資料夾。
- GitHub 只讀查核確認遠端 `agent/official-site-v1` 仍落後於本機候選，且沒有正式網域／Pages 設定證據；本輪沒有 push。
- GitHub Pages 標準網址只讀探測回傳 HTTP `404`；沒有公開 URL 可供正式 SEO/GEO 或候補 readback 驗收。
- 未部署至公開網址、未送出表單、未建立外部帳號、未讀取真實客戶／學生資料，亦未修改 Cloudflare、付款、secrets、授權或 `l2_current_truth.json`。

## 回滾

本輪檔案均位於候選資料夾。若需撤回，可還原本次變更檔案或回到上一個候選版本；若日後另行推送，使用 Git revert 撤回候選 commit。不影響正式網站、Cloudflare、後台、付款、授權或共同真相。

## 2026-07-13 正式公開驗收結果（以本節為最新狀態）

- 正式公開網址：`https://www.dealalliancehub.com`。
- Cloudflare Pages 預設網址：`https://deal-alliance-official-site.pages.dev`，已回應 HTTPS `200`。
- 根網址：`https://dealalliancehub.com` 已以 `301` 導向正式 `www` 網址。
- 公開 DNS：Cloudflare 公開解析器已回傳 `www.dealalliancehub.com` 的 edge IP；正式 HTTPS 首頁實測 `200`，並讀到 `Content-Security-Policy`、`X-Content-Type-Options`、`Referrer-Policy`。
- 原始正式驗收器已實測通過：`PASS_PUBLIC_RELEASE origin=https://www.dealalliancehub.com paths=19 root_redirect=www crawler_assets=ok waitlist_post=not_performed resolver=public_doh+system_dns`。
- 為避免本機 Tailscale／系統 DNS 短暫負向快取造成假失敗，`tests/verify_public_release.py` 已改為系統 DNS 失敗時改用公開 DNS-over-HTTPS；它仍只做 GET、仍不提交候補表單，也不讀取任何私人端點。
- 候補表單仍維持 disabled：沒有 receiver、沒有真實名單、沒有登入／付款／下載啟用宣稱。

## 2026-07-13 註冊與 SEO/GEO 最終複盤

唯一總目標：**讓訪客從正式官網找到正確的註冊／登入入口，安全進入後台，完成註冊後只看到自己被授權的服務；官網不保存密碼、session、token、學生資料或帳務資料。**

- 正式公開只讀 Gate：已通過 `PASS_PUBLIC_RELEASE origin=https://www.dealalliancehub.com paths=19 root_redirect=www crawler_assets=ok waitlist_post=not_performed resolver=public_doh+system_dns`。
- 技術 SEO/GEO：已完成並通過公開 readback，包括 HTTPS、canonical、title、description、OG、JSON-LD、sitemap、robots、`llms.txt`、公開安全標頭與可抓取路由。
- `llms.txt` 已校正為正式公開網址與實際服務邊界，不再保留「尚未部署」的過期說法；新的公開 readback Gate 會拒絕過期文字、缺公開導覽或缺帳號資料邊界的版本。
- SEO/GEO 不能宣稱「永久完成」：Search Console、Bing 收錄、AI 實際引用、Core Web Vitals 與轉換數據是上線後持續量測，不保證排名或引用。
- 官網目前缺口：尚無核准的後台註冊／登入 URL、欄位與授權契約，因此不製作假註冊成功頁或猜測 endpoint。
- 已建立 [官網註冊入口整合契約_2026-07-13.md](./官網註冊入口整合契約_2026-07-13.md)，由主窗口提供後台介面後，再補官網 CTA 與跨系統假帳號 E2E。

以上證據只確認「公開官網」已正式可用；學生後台、付款、工具授權、下載、App 簽章與真實候補 receiver 仍是其他工作線，未因官網發布而自動啟用。

## 2026-07-13 註冊入口元件先行完成

- 已在公開候選加入可維護的「建立帳號／登入我的工具」兩個導覽元件，但目前預設 `disabled`、沒有 URL、沒有允許 Origin，因此公開頁不會顯示假的登入按鈕。
- 啟用條件為：主窗口提供已讀回的 HTTPS 後台 URL，並把其 Origin 明確列入白名單；元件只做導向，不處理帳密、session、token 或任何帳號資料。
- 本機安全 Gate 會驗證預設關閉、HTTPS／白名單 guard、禁止 `fetch(config.accountPortalUrl)`，避免把公開站變成帳號資料入口。
- `tests/verify_account_portal_runtime.mjs` 會以 `example.invalid` 假網址實際執行同一份前端程式：disabled、缺一網址、錯 Origin、HTTP 一律不顯示；兩個核准 HTTPS 假網址才會同時產生「建立帳號／登入我的工具」連結。
- 所有 15 個公開頁與 404 都必須先載入 `site-config.js`、再載入 `site.js`；本機與公開 readback Gate 都會拒絕設定檔漏載或順序錯誤的版本。
- 尚未能執行「註冊成功／登入失敗／忘記密碼」的假帳號 E2E，原因是後台測試 URL 與流程契約尚未回傳；沒有用假頁面冒充完成。

## 2026-07-13 最新公開候選同步讀回（以本節為唯一最新部署狀態）

- 本機候選完整 Gate 已通過：`PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com public_release_verified=true registration_pending=true`。
- 本機假資料驗證已通過：帳號入口在 `disabled`、URL 不完整、非白名單或 HTTP 時一律不顯示；只有兩個核准的 HTTPS `example.invalid` 假網址才顯示兩個純導向連結。候補 fake receiver 回傳 `202`、穩定去重，且預設保持 `disabled`。
- 對目前正式網址執行 GET-only `tests/verify_public_release.py` 的結果為：`FAIL_PUBLIC_RELEASE path=/ account_portal_bootstrap_missing`。
- 判讀：正式首頁尚未載入 `/assets/site-config.js`（或沒有在 `/assets/site.js` 前載入），表示 live artifact 仍是較舊版本；這不是 DNS、Cloudflare 安全標頭或候選程式失敗。
- 對外狀態必須標為：`PUBLIC_RELEASE_ARTIFACT_OUT_OF_SYNC_PENDING_DEPLOY_SYNC`。在主窗口將現行 `dist/` 同步發布、且重新取得 `PASS_PUBLIC_RELEASE` 前，不可把本次帳號入口／`llms.txt` 修正宣稱為已在正式站生效。
- 官網仍不會顯示帳號入口，因候選設定預設 `accountPortalMode: "disabled"`、註冊／登入 URL 空白；即使 artifact 同步，也不會收帳密或導向未驗證後台。

## 2026-07-14 工具事實同步與候選 Artifact 重新驗收

- 權威工具表的 SHA-256 已更新為 `2485b6d04c82812cce4e17f48d1bb156ac93ea34fbcf186bc52aad3d1f9d87d8`。LINE 的唯一現況 marker 為 `MAC_COMPILED_V3_CANDIDATE_PRE_SIGN_GUI_PENDING`，取代本報告較早的 V2 描述。
- 對外文案仍是 `WAITLIST_ONLY`：LINE 只寫「候補中・不可下載，V3 實機驗收中」，不出現下載、登入或發送入口；舊的已公證 DMG 仍因 `MAC-PACKAGE-001` 不可交付。V3 的 GUI、無發送實機、重簽與重公證都尚未完成。
- 已由最新 source 完整重建 `dist/`，並重跑 release contract、headers、候補 fake E2E、SEO/schema、公開邊界、工具狀態快照、sitemap/mobile、llms、account portal bootstrap、假訪客與本機 HTTP Gate，全部 PASS；account portal runtime 亦確認只有兩個核准的 HTTPS 假網址才會顯示純導向連結。
- 這是本機候選驗證，不是新的公開部署。正式官網仍需由主窗口受控同步最新 `dist/` 後，重新以 GET-only `tests/verify_public_release.py` 讀回；在取得 PASS 前，不把 V3 文案、`llms.txt` 或 bootstrap 修正說成已在公開站生效。

## 2026-07-14 正式發布與公開讀回（以本節為最新狀態）

- 主窗口已推送 commit `94e0170`，並在乾淨發布工作樹重跑全套 Gate 成功。
- 第一次手動 workflow 暴露 GitHub Pages 尚未啟用；未跳過錯誤。啟用 GitHub Actions 為 Pages source 後，第二次 workflow 的 validate 與 deploy 都成功。
- Cloudflare Pages 已同步同一提交。正式網址的 GET-only readback 已通過：`PASS_PUBLIC_RELEASE origin=https://www.dealalliancehub.com paths=19 root_redirect=www crawler_assets=ok waitlist_post=not_performed resolver=public_doh+system_dns`。
- 正式站目前已包含 V3 候補文字、`site-config.js` bootstrap 與更新過的 `llms.txt`；帳號入口及候補 receiver 仍為空白／`disabled`，沒有收集或傳送任何真實資料。

## 2026-07-14 LINE V4 候補狀態待下一次受控同步

- 主窗口最新獨立驗證為 LINE native V4：47 項假資料 core、static no-send 與 concrete driver source typecheck 通過；driver 尚未接入 UI 或執行。
- 權威表與候選 `dist/` 已改為 `MAC_COMPILED_V4_CANDIDATE_PRE_SIGN_GUI_PENDING`／「候補中・不可下載，V4 實機驗收中」。這仍是候補資訊，沒有下載、登入或發送入口。
- 本節更新後尚未推送／部署；下一次同步必須先重跑同一套乾淨工作樹 Gate、再發布並以正式 HTTPS GET-only Gate 讀回。

## 2026-07-14 LINE V4 候補同步已發布並讀回（以本節為最新狀態）

- 現行受控發布提交為 `3c436b32cbdbf6e5d6c547c9720fbb72d097e6fe`（簡稱 `3c436b3`），只同步已驗證的公開候補事實與網站 artifact；沒有啟用帳號入口、候補 receiver、付款、下載或工具發送。
- 發布前在乾淨 Git 工作樹重跑完整 Gate，所有 Gate 通過；其中工具狀態來源為版本化且非公開的 release snapshot，隔離 checkout 也能重跑，不依賴主專案私有檔案或環境變數。
- GitHub Pages 手動發布 workflow #3 的 `validate` 與 `deploy` 都成功，Pages artifact SHA-256 為 `27d70cb0b9e5255076671fda263ad1e0a7861cbbde2cadfd87b7c1c1403df7ba`。GitHub Pages 只作發布備援，公開 canonical 仍為 `https://www.dealalliancehub.com`。
- 正式 Cloudflare 網站的 LINE 工具頁已以 HTTPS 只讀讀回：顯示「候補中・不可下載，V4 實機驗收中」與「編譯式 V4 候選」。這只確認公開說法已同步，**不代表 LINE App 已完成 GUI、真實 LINE、簽章、公證、Windows 或後台授權驗收**。
- 正式公開站仍維持 fail-closed：帳號入口與候補 receiver 的 URL 為空、模式為 `disabled`；官網不收帳密、session、token、學生資料、帳務資料或候補名單。

## 2026-07-14 公開首頁視覺與行動版讀回

- 以正式 `https://www.dealalliancehub.com/` 實際畫面查核，桌面首頁的品牌導覽、主標題、三個問題導向入口與「取得開放通知」CTA 均可見且層級清楚；這是公開頁的唯讀視覺確認，不是 mock 或原始碼截圖。
- 以 `390 × 844` 行動版 viewport 檢查，DOM 回傳 `innerWidth=390`、`documentElement.scrollWidth=390`、`body.scrollWidth=390`，且 viewport meta 為 `width=device-width, initial-scale=1`；未發現水平溢出。
- 本機 Browser 截圖工具在行動 viewport 顯示時出現自身畫布留白，但 DOM 寬度與 overflow 證據正常；因此本節只宣稱「響應式寬度／無橫向溢出已讀回」，不把該截圖工具的畫布留白誤寫成網站版面問題或完整行動視覺驗收。
- 本次未修改公開網站、未操作任何表單、未登入、未讀取或傳送真實資料。若要回滾本節，只需移除此段驗收紀錄；不影響站點或外部服務。

## 2026-07-14 帳號入口敏感 URL 防呆（候選，未部署）

- 修改檔案：`assets/site.js`、`tests/verify_account_portal_runtime.mjs`、`tests/verify_site.py`，並以本節記錄驗收結果。
- 公開官網帳號 CTA 除了既有的 HTTPS、明確 Origin 白名單與 `enabled` 模式外，現在只接受乾淨的 `/register` 與 `/login` URL；任何 query string、fragment、URL 內帳密或其他路徑都不會渲染連結。這避免把 reset token、session、密碼或內部 App 路徑意外公開到訪客頁。
- 去識別假資料端到端證據：`verify_account_portal_runtime.mjs` 實際執行同一份前端程式，已驗證 disabled、缺 URL、錯 Origin、HTTP、帶 token、帶 fragment、帶 URL 帳密與錯誤路徑全部隱藏；只有 `example.invalid/register`＋`example.invalid/login` 的乾淨 HTTPS 假網址才會同時產生兩個純導向連結。輸出：`PASS_ACCOUNT_PORTAL_RUNTIME ... sensitive_url=hidden approved_https_clean_paths=two_links fake_urls_only=true`。
- 完整本機 Gate 證據：`build-public.sh`、release contract、headers contract、候補 fake E2E、SEO/schema、公開邊界、工具狀態對齊、sitemap/mobile、`llms.txt`、account bootstrap、假訪客與本機 HTTP 路由皆 PASS；總結為 `PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com public_release_verified=true registration_pending=true`。
- 尚未完成／風險：`app.dealalliancehub.com` 尚未由公開 DNS 解析（本節撰寫時的 GET-only `/register`、`/login` 讀取皆為 `Could not resolve host`），故 CTA 保持 disabled；也尚未取得後台的去識別註冊、驗證、取消、登入、重設、登出與權益 allow/deny 生命週期 readback。不得把本項候選防呆說成後台已可用。
- 未執行外部動作：未部署、push、修改 Cloudflare/DNS/Pages/Worker、登入、建立真實帳號、送出表單或接收真實資料。
- 回滾：還原上述三個候選檔案，重新執行 `bash scripts/build-public.sh` 與完整 Gate；因未部署，不影響正式站或任何外部服務。

## 2026-07-14 App staging 公開讀回：網域可達但帳號頁未就緒

- Cloudflare DNS-over-HTTPS 對 `app.dealalliancehub.com` 回覆 `Status: 0` 與兩個公開 A 記錄；代表 Custom Domain 已進入公開解析，而本機系統 DNS 仍可能暫存舊的 NXDOMAIN。
- 以公開 DNS IP 的 SNI HTTPS GET-only 讀回，`/register` 與 `/login` 都可連線，但目前均回傳 `HTTP/2 404`。兩者已有 `Cache-Control: no-store` 與 `X-Content-Type-Options: nosniff`，但本次標頭中未讀到 `X-Robots-Tag`。
- 判讀：**網域存在不等於後台可用。** 在兩條帳號頁回覆成功頁面、具備 `no-store`／`X-Robots-Tag: noindex` 私有標頭，並完成去識別帳號生命週期驗收前，公開官網保持 `accountPortalMode: "disabled"`，不可顯示建立帳號或登入連結。
- 未執行外部變更、帳號建立、登入或表單送出；上述均為 GET-only／DNS 查詢讀回。回滾不需要外部操作；若需撤回本節，只移除本段驗收紀錄即可。

## 2026-07-14 註冊入口整合規格校正（候選文件）

- 修改檔案：`官網註冊入口整合契約_2026-07-13.md`、`README.md`。
- 已移除舊的「公開 artifact 尚未同步」與 `/register/` 範例，統一為唯一可啟用的純 HTTPS URL：`https://app.dealalliancehub.com/register`、`https://app.dealalliancehub.com/login`。兩者不可附帶 query、fragment、URL 內帳密、token 或其他 path；忘記密碼與驗證失敗由後台自行處理，不能把敏感狀態帶回公開官網。
- 整合契約新增明確 Gate：兩條 URL 必須 GET `200`、有 `Cache-Control: no-store` 與 `X-Robots-Tag: noindex`，再加上去識別帳號完整生命週期 readback，才可將官網 CTA 從 disabled 改為 enabled。
- 文件交叉檢查：搜尋已確認上述兩個文件不再含舊 artifact 狀態、舊尾端斜線範例或「`site-config.js` 尚未同步」說法。
- 未完成／風險：Staging `/register`、`/login` 仍須由後台線實作為私有 `200` 頁面並完成 fake lifecycle；本節不表示帳號入口已啟用。
- 未執行外部動作：未部署、push、修改 Cloudflare/DNS/Pages/Worker、登入、建立帳號或提交表單。回滾只需還原上述兩份候選文件；不影響公開站或外部服務。

## 2026-07-14 公開內容複盤與生涯運數隱藏（候選，未部署）

- 修改檔案：`tools/catalog.json`、`tools/index.html`、`sitemap.xml`、`release_contract.json`、`scripts/build-public.sh`、`tests/verify_site.py`、`tests/verify_public_release.py`、`assets/site.js`、候補／FAQ／隱私／條款頁、README、公開推播前交接清單，以及 [公開內容資料複盤與待確認清單_2026-07-14.md](./公開內容資料複盤與待確認清單_2026-07-14.md)。
- 生涯運數計算器已從候選的公開 catalog、工具總覽、sitemap、發布路徑與公開驗收清單移除；原始候選頁仍保留在 source，但 `build-public.sh` 不會複製它到 `dist/`。本機輸出：`PASS_HIDDEN_TOOL_PUBLIC_ARTIFACT tool=life-number-calculator excluded=true`。
- 現行正式網址以唯讀讀回確認仍包含「生涯運數計算器」與「全部工具公開總覽」；原因是本輪未部署，這不是候選 Gate 偽 PASS。發布後必須重新執行公開 GET-only Gate，確認 sitemap 從 15 降為 14 個公開 URL 且舊路徑不再被發布。
- 已把候補頁、FAQ、隱私頁與條款頁從「本機／候選站」校正為目前公開網站的真實邊界：帳號、付款與候補收件尚未啟用；不建立名單或傳送表單資料。完整 owner 待確認資料清單已列在新複盤文件，包含營運主體、公開客服、隱私聯絡人、地區／準據法、品牌定位與首批公開資源。
- 去識別假資料驗收：候補 fake E2E 仍回傳 `202`、穩定去重與 `400` 拒絕測試；預設 receiver 仍 disabled。完整本機 Gate 全部 PASS：metadata/schema `pages=14`、公開工具 `tool_catalog=5 hidden_tool=life-number-calculator`、sitemap `14`、account bootstrap `15`、local HTTP `19`。
- 未完成／風險：候選變更尚未受控發布；正式隱私／條款所需的公司主體、客服、資料控制者與正式服務規則尚未由 owner 確認。帳號後台 fake lifecycle 也仍未完成，因此 CTA 保持 disabled。
- 未執行外部動作：未部署、push、修改 Cloudflare/DNS/Pages/Worker、登入、建立帳號、送出表單或讀取真實資料。回滾方式：還原本節列出的候選檔案，執行 `bash scripts/build-public.sh` 與完整 Gate；因未部署，不影響現行公開站或外部服務。

## 2026-07-14 帳號 CTA 候選接線完成（以本節為最新）

- 目前階段：`PENDING_USER_ACCEPTANCE`。主窗口已完成後台 staging 的去識別帳號、權益與四個 App OAuth 讀回；公開官網候選現只啟用兩個純導向連結：`https://app.dealalliancehub.com/register`、`https://app.dealalliancehub.com/login`，唯一 allowlist Origin 為 `https://app.dealalliancehub.com`。
- 修改檔案：`assets/site-config.js`、`site.config.json`、`release_contract.json`、`assets/site.js`、`tests/verify_site.py`、`tests/verify_release_contract.py`、新增 `tests/verify_account_portal_public.py`、`README.md`、`官網註冊入口整合契約_2026-07-13.md`、`公開內容資料複盤與待確認清單_2026-07-14.md` 與本報告。
- 去識別端到端／跨系統證據：後台 lifecycle readback 位於 `內部工作資料/驗證資料/2026-07-14_core_app_staging_oauth_and_account_lifecycle_readback.md`；官網端實跑 `PASS_ACCOUNT_PORTAL_RUNTIME ... invalid_origin=hidden http=hidden sensitive_url=hidden approved_https_clean_paths=two_links fake_urls_only=true`、`PASS_ACCOUNT_PORTAL_PUBLIC origin=https://app.dealalliancehub.com paths=2 private_headers=ok credential_forms=absent resolver=explicit_sni_dns_pin`，並通過完整本機 Gate。
- 完整本機結果：release contract（18 routes、7 monitoring）、headers contract、候補 fake E2E、metadata/schema、公開邊界、工具來源、sitemap、`llms.txt`、account bootstrap、fake visitor、local HTTP 均 PASS；最終輸出 `PASS_ALL_LOCAL_GATES ... account_portal_candidate_enabled=true user_acceptance_pending=true`。`dist/tools/life-number-calculator` 不存在，隱藏 Gate PASS。
- 未完成／風險：候選尚未發布，因此目前正式網址仍未有這兩個 CTA，也仍可能顯示舊的生涯運數工具與過渡文案。staging 私有頁無 credential form 是測試安全邊界，不可稱為「真實公開註冊」；付款、下載、receiver、真實帳號與真實工具發送一律未啟用。仍缺 owner 的正式營運主體、公開客服與完整隱私／條款資料。
- 未執行外部動作：未部署、push、修改 Cloudflare/DNS/Pages/Worker、建立帳號、登入、提交表單、傳遞帳密／session／token 或讀取真實客戶資料；唯一外部動作是兩個 staging URL 的 GET-only 安全標頭讀取。
- 回滾：將 `assets/site-config.js` 的兩個 URL 清空並把 `accountPortalMode` 改回 `disabled`，同步還原 `site.config.json`、`release_contract.json` 和驗收預期後，執行 `bash scripts/build-public.sh`、`python3 tests/verify_release_contract.py`、`python3 tests/verify_headers_contract.py`、`<bundled-node> tests/verify_account_portal_runtime.mjs` 與 `python3 tests/verify_site.py`。因本輪沒有部署，回滾不影響現行公開站。

## 2026-07-14 公開文案與全頁帳號入口一致性（以本節為最新）

- 工作範圍：只修正公開官網候選與驗收；未碰後台、Cloudflare、付款、receiver、學生／客戶資料、secrets 或正式 live。
- 修改檔案：首頁、about、solutions、FAQ、privacy、404、search、五個公開工具頁的公開文案；`tests/verify_site.py`；以及此報告。候選 `dist/` 已由 `build-public.sh` 重建。
- 修正內容：移除公開頁的「候選站 v1／本候選站／沒有登入」等過期說法，統一為「公開頁不處理帳號資料，會員帳號只由安全入口處理」。工具、付款、下載、receiver 與真實資料處理仍維持未開放。404 與 noindex 搜尋空頁現在也有相同導覽，能顯示經 allowlist 保護的帳號 CTA。
- 去識別端到端證據：`PASS_ACCOUNT_PORTAL_RUNTIME ... sensitive_url=hidden approved_https_clean_paths=two_links fake_urls_only=true`；本機假資料候補流程仍 `PASS_WAITLIST_FAKE_E2E status=202 ... default_mode=disabled`，不持久化或外送測資。
- Gate：`PASS_ACCOUNT_PORTAL_BOOTSTRAP pages=16 artifacts=source+dist nav=all`、`PASS_PUBLIC_COPY_INTEGRITY routes=16 account_portal_language=consistent`，以及完整 local Gate 均 PASS。跨系統唯讀驗證亦通過：`PASS_ACCOUNT_PORTAL_PUBLIC ... paths=2 private_headers=ok credential_forms=absent`。
- 未完成／風險：尚未部署，所以正式網址仍不是本節候選內容。正式隱私／條款仍缺營運主體、公開客服、資料控制者、適用地區與正式服務規則；公開資源也仍待 owner 提供可公開來源。不得把 staging 私有頁或候選 CTA 稱作真實公開註冊。
- 未執行外部動作：未部署、push、送出表單、登入、建立帳號、傳遞帳密／token、讀取真實資料或改動 Cloudflare。唯一外部操作為 `/register`、`/login` 的 GET-only 安全讀回。
- 回滾：回復本節列出的公開 HTML 和 `tests/verify_site.py`，重跑 `bash scripts/build-public.sh` 與完整 Gate；本輪未發布，故不影響 live。
- 今天學到什麼：公開頁的「安全邊界」不能寫成「沒有帳號服務」。根因是 CTA 狀態由 `site-config.js` 更新後，歷史公開文案沒有同輪同步。未來防呆應放進共用 `deal-alliance-product-delivery` Skill 的網站 Gate：每次 CTA 狀態切換，都必須掃描所有公開、404 與搜尋頁的登入／註冊敘述，並確認每頁同時載入 config、site script 和 nav。

## 2026-07-14 候補工具 SEO/GEO 事實校正（以本節為最新）

- 工作範圍：只更新官網候選的公開 schema、sitemap、條款說明與 Gate；未變動後台、工具程式、帳號、付款、receiver 或任何外部服務。
- 根因：跟進節奏工具頁雖可見標示為候補，JSON-LD 卻是 `SoftwareApplication` 並含 `isAccessibleForFree: true`。這會讓搜尋引擎或 AI 將「候補公開說明」誤讀為「免費可使用的軟體」。
- 修改檔案：`tools/follow-up-rhythm/index.html`（改為描述性 `WebPage` schema，移除免費可用宣稱）、`terms/index.html`、`sitemap.xml`（全數公開路由更新為 `2026-07-14` lastmod）、`tests/verify_site.py` 與本報告。
- 驗證：候補工具頁必須是 `WebPage` schema，且不得含 `SoftwareApplication`、`Product`、`Offer`、`offers` 或 `isAccessibleForFree`。完整本機 Gate 實跑 PASS：`PASS_PUBLIC_BOUNDARY ... waitlist_schema=descriptive_only ...`、`PASS_SITEMAP_AND_MOBILE sitemap_urls=14 lastmod=complete`，其他安全、假資料 E2E、SEO/GEO 與 HTTP Gate 亦全部 PASS。
- 未完成／風險：這不是工具上架證據；五個工具仍全為 `WAITLIST_ONLY`，且 candidate 尚未發布。Search Console／Bing、真實收錄與 AI 引用仍需在受控發布後以正式網址量測。正式隱私／條款與公開資源來源仍待 owner 資料。
- 未執行外部動作：未部署、push、登入、提交表單、建立帳號、讀取真實資料或變更 Cloudflare。回滾方式：還原本節四個候選檔案、重建 `dist/` 並重跑完整 Gate；本輪未發布，不影響 live。
- 今天學到什麼：可見文案與 JSON-LD 必須被視為同一份公開承諾。未來防呆建議主窗口回寫到 `deal-alliance-product-delivery` Skill 的 SEO/GEO Gate：任何 `WAITLIST_ONLY`／`NOT_ENABLED` 工具一律不得輸出 `SoftwareApplication`、`Product`、Offer／價格或免費可用欄位；sitemap 每個公開路由必須有可驗證格式的 `lastmod`。

## 2026-07-14 正式可營運資料 Gate（以本節為最新）

- 工作範圍：只增加候選官網的 release／營運防呆與驗收文件；不填寫或猜測營運主體、Email、法律內容、客服資料，也不修改後台或外部設定。
- 修改檔案：`release_contract.json`、`tests/verify_release_contract.py`、新增 `tests/verify_operational_readiness.py`、`tests/verify_public_release.py`、`README.md` 與本報告。
- 修正內容：release contract 現明確區分「技術候選可受控發布」與「正式可營運服務」。在 owner 尚未核准營運主體、公開客服、隱私聯絡人、隱私／條款版本與生效日、服務地區／準據法，以及具作者／來源／日期的首批公開資源前，`operational_release_ready` 必定是 `false`，不可稱為正式可營運。
- 去識別／本機驗證：`PASS_OPERATIONAL_READINESS_GUARD formal_operation=false owner_inputs_pending=7`；release contract、headers、CTA runtime、候補 fake E2E、SEO/GEO、工具狀態、sitemap、local HTTP Gate 全部 PASS。四個 release verifier 以不寫檔 AST 檢查通過：`PASS_RELEASE_GATE_SYNTAX verifiers=4`。
- 部署後新增 readback：`verify_public_release.py` 現會讀取正式 `assets/site-config.js`，確認 register、login、allowlist 與 enabled 狀態都與核准候選完全相同；若 live 仍是舊 disabled config，Gate 必須失敗，而不是把 script 載入本身當作 CTA 已上線。
- 未完成／風險：此 Gate 證明「尚不可稱正式可營運」，不是替代 owner 資料、正式法律審閱、Search Console／Bing、監控實際設定、受控發布與使用者驗收。候選仍未部署；所有真實註冊、付款、下載、receiver 和資料收集仍 disabled。
- 未執行外部動作：未部署、push、登入、建立帳號、送表單、讀取真實資料或修改 Cloudflare。回滾：還原本節列出的候選檔案並重跑 Gate；沒有外部影響。
- 今天學到什麼：技術 readback PASS 不等於業務／法務 readiness。未來防呆建議回寫 `deal-alliance-product-delivery` Skill：每次 release contract 都要列出可驗證的 owner 公開資料缺口，並用 machine-readable `release_ready: false` 阻止把候選誤稱正式營運。

## 2026-07-14 最終目標完成度稽核（以本節為最新）

- 工作範圍：新增候選官網的目標追蹤矩陣與 machine-readable 稽核；未更動公開服務、後台、帳號、付款、receiver 或外部系統。
- 修改檔案：新增 `正式營運完成度矩陣_2026-07-14.md`、新增 `tests/verify_goal_readiness.py`、更新 `README.md` 與本報告。
- 稽核將唯一目標拆成六件可驗證的事：訪客理解、帳號入口、授權服務邊界、SEO/GEO、隱私／安全／監控／回滾、正式可營運。每一項都有現有證據、真實狀態與下一位負責者，避免將 local PASS 等同正式完成。
- 去識別／本機證據：`PASS_GOAL_READINESS_AUDIT visitor_understanding=candidate_ready registration=candidate_ready authorized_services=waitlist_only seo_geo=technical_gate_ready operational=false monitoring=owner_pending release=false`；完整候選 Gate 同輪全部 PASS。
- 未完成／風險：這份結果刻意確認網站仍未達正式目標，因為受控發布／正式 readback、owner 七項公開資料、外部監控設定、Search Console／Bing 量測與使用者驗收尚未完成。不得把此稽核輸出的 `PASS` 解讀為 `RELEASE_READY`。
- 未執行外部動作：未部署、push、登入、建立帳號、送出表單、讀取真實資料、修改 Cloudflare 或發送任何資料。回滾只需移除新增矩陣與稽核檔並重跑完整 Gate；沒有外部影響。
- 今天學到什麼：最終目標需要有「正確未完成」的機器證據，而不是只有成功測試清單。建議主窗口將完成度矩陣的模式納入共同 release 報告：所有候選產品要列出 `candidate_ready`、`operational_ready`、`release_ready` 三個不同層級，並禁止跨級宣稱。
