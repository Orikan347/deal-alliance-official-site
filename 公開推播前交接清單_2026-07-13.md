# 成交聯盟官方網站｜公開推播前交接清單

版本：`v1.0`
目前階段：`PENDING_RELEASE_SECURITY`
狀態：`PUBLIC_RELEASE_VERIFIED_PENDING_REGISTRATION`
作品 ID：`ORI-WEB-DA-OFFICIAL-MVP-20260713-V1`  
範圍：只適用 `200_官網整合/公開官網候選_v1/`；本文件不是部署授權。

最新狀態：正式公開網址的基礎路由曾完成只讀驗證；但目前 live artifact 尚未同步帳號入口 bootstrap，必須先完成候選 `dist/` 的受控發布同步並重新 readback。同步後才進入官網導向核准後台註冊／登入的主線；目前仍未接真實 receiver 或帳號資料。

## 一、目前已完成

- 公開頁：首頁、About、Solutions、Tools、工具詳情、Resources、FAQ、候補／洽詢、Privacy、Terms、404、搜尋空狀態。
- 品牌：成交聯盟官方色彩、工具箱＋勾線＋星芒 Logo、共用樣式與一致導覽。
- 公開邊界：六項工具均沒有正式登入、付款、授權或真實資料；四項工具提供本機假資料示範，正式服務仍標示候補／洽詢。
- SEO／GEO 本機準備：title、description、canonical、OG、JSON-LD、sitemap、robots、`llms.txt`、可見答案與結構化資料一致。
- 假資料 E2E：使用 `visitor_problem_01`、`test-visitor@example.invalid` 等假資料；互動只在本機顯示結果，不保存、不傳送。
- 候補收件契約：`waitlist_contract.json` 定義最小欄位、禁止欄位、202/readback、Origin 白名單、限流、保存與刪除 Gate；`assets/site-config.js` 預設 fail-closed。
- 假 receiver E2E：`python3 tests/verify_waitlist_contract.py` 通過成功 202/readback、重複請求穩定去重與錯誤 400 拒絕；沒有連接真實收件服務。
- Codex 瀏覽器功能 E2E：已測聯絡人轉換成功／錯誤、Smart Close 本機整理、簡訊預覽、LINE 四步驟流程預覽；結果均未上傳、未保存、未登入、未發送。
- 核心控制補齊：示範與候補表單均有清除／取消；聯絡人轉換對缺欄位、空值與錯誤 Email 會拒絕並保持不可下載。
- 本機瀏覽器 readback：首頁 DOM 正常渲染；候補頁以 `visitor@example.invalid` 測試同意／本機示意、未勾選拒絕與清除／取消，均未送出、未保存、未建立名單。
- 本輪 UX readback：404 已改為完整共用 shell，含導覽、麵包屑、回首頁／工具 CTA 與頁尾；About／聯絡人轉換頁的次要 CTA 已補上品牌樣式；候補未勾選同意時由 required 防線阻止，勾選假 Email 後仍只顯示本機未送出訊息。
- 可及性補強：加入 `prefers-reduced-motion` 規則，使用者偏好減少動態時停用頁面平滑捲動與 hover 位移。
- 手機設計補強：390px viewport 下主導覽不再消失，改為可橫向滑動並保留首頁、解決方案、工具、公開資源、FAQ 與候補 CTA；已以瀏覽器 readback 確認可見。
- 同意防線：候補表單在本機與未來正式模式均要求勾選同意；未勾選時由必要核取方塊阻止提交，勾選假 Email 只產生本機結果。
- 靜態安全標頭候選：`_headers` 已加入 `nosniff`、嚴格 referrer、權限限制、CSP、同源連線、同源表單與禁止 frame；正式 host 需做實際 response header readback。
- 發布契約：`release_contract.json` 與 `tests/verify_release_contract.py` 已把 19 個公開路徑、監控、候補 readback 與回滾條件機器化；正式 domain／receiver／外部監控仍為待核准狀態。
- 公開 readback Gate：部署並完成 DNS 後，主窗口可執行 `python3 tests/verify_public_release.py`；此 Gate 只做 HTTPS／路由／metadata／crawler 資產／安全標頭 GET，不會送出候補資料。
- GitHub Pages 發布準備：`.github/workflows/pages-manual-release.yml` 只接受 `workflow_dispatch`，先跑候選 Gate，再使用最小 Pages 權限部署；目前未推送、未觸發，且發布核准仍未完成。
- GitHub 只讀查核（2026-07-13）：`Orikan347/deal-alliance-official-site` 存在，預設分支為 `agent/official-site-v1`；遠端仍是舊候選，未查得 Pages workflow、CNAME 或正式公開網址。此查核沒有修改、commit 或 push。
- GitHub Pages 標準網址只讀 HTTP readback：`https://orikan347.github.io/deal-alliance-official-site/` 回傳 `404`（2026-07-13）；因此不能把它當成官方公開網址。
- 正式網域只讀檢查（2026-07-13）：`curl -I -L https://www.dealalliancehub.com/` 與根網域均回傳 `Could not resolve host`；目前沒有公開 DNS／HTTPS readback，不能宣稱已上線。
- 使用者驗收尚未完成：需由使用者親自測試輸入、讀取、篩選、編輯、預覽、取消、錯誤與完成結果。
- 回滾：候選檔案均在本資料夾；保留候選版本即可撤回，不影響 Cloudflare、後台、付款、授權、secrets 或真實資料。

## 二、主窗口必須先提供／核准

主窗口已確認正式公開來源為 `https://www.dealalliancehub.com`；目前尚未部署，因此外部 HTTPS、SEO／GEO crawler、分享卡與收錄仍不可宣稱已驗證。本機 metadata 已切換到正式來源。

| 項目 | 目前狀態 | 核准後動作 |
|---|---|---|
| 正式 HTTPS 網域 | `OWNER_CONFIRMED_NOT_DEPLOYED` | 部署後做公開 DNS／HTTPS／canonical／sitemap／OG／JSON-LD／`llms.txt` readback |
| 公開品牌文字 | `PENDING_OWNER_REVIEW` | 核對品牌名稱、Logo、對外定位與聯絡入口 |
| 工具公開清單與狀態 | `PENDING_FACT_REVIEW` | 每項確認 `source_ref`、`offer_status`、`last_reviewed` |
| 公開資源與作者資訊 | `PENDING` | 只加入已核准、可公開查證的內容 |
| 候補／洽詢欄位與同意文字 | `PENDING_PRIVACY_REVIEW` | 先完成目的、保存、刪除、防濫用與 readback 規則 |
| 候補收件 HTTPS 端點 | `PENDING_ENDPOINT` | 提供正式端點、Origin 白名單、202/readback 與 operator-only 查詢證據；不得把 public list endpoint 暴露給前端 |
| 正式部署位置與回滾點 | `PENDING_RELEASE_OWNER` | 由獲授權主窗口執行部署與回滾 |

候補／洽詢目前也沒有可直接接上的正式收件端點；候選頁的 `site.js` 僅做本機 `preventDefault` readback，正式串接必須另有核准的後端、同意紀錄、保存／刪除規則與外部驗收證據。

## 三、正式 staging 驗收順序

1. 先在隔離 staging 替換正式網域佔位，不收真實名單。
2. 驗證所有路由的 HTTPS、手機版、404、內部連結與 canonical。
3. 驗證 OG 分享卡、JSON-LD、Rich Results、robots、sitemap、`llms.txt`。
4. 以只讀方式確認公開 crawler、OAI-SearchBot、Google Search Console、Bing Webmaster Tools 與 WAF 狀態。
5. 做一次假資料候補流程，確認沒有送出、保存或外部寫入。
6. 由主窗口確認版本 hash、部署紀錄、回滾點與發布時間，再決定是否公開。

## 四、禁止直接做的事

- 不得把 `official-domain-pending.invalid` 留在任何公開頁或 metadata。
- 不得把候補工具改寫成已上線、可購買、可下載、可試用或可登入。
- 不得把本機示意表單接到 Email、LINE、CRM、資料庫、付款或任何外部 API。
- 不得在未核准前加入客戶、學生、聯絡人、帳務或授權資料。
- 本聊天室目前不執行部署、Cloudflare、付款、secrets、外部帳號或正式發布；GitHub 候選分支已依使用者明確要求完成推送，但不代表已公開上線。

## 五、交接證據

- 本機網站驗收：`PASS_ALL_LOCAL_GATES`。
- SEO／GEO Gate：`PASS_LOCAL_PREPARATION_PENDING_EXTERNAL`，`error_count=0`；外部檢查仍為 `PENDING`。
- E2E：`PASS_E2E`；verified fixture accepted、unverified claim and PII blocked、real candidate remains safely pending。
- 詳細驗收：[驗收報告_2026-07-13.md](./驗收報告_2026-07-13.md)。
- 完整設計與 Gate：[網站完整規劃藍圖_2026-07-13.md](./網站完整規劃藍圖_2026-07-13.md)。

## 六、正式監控與回滾契約

- 每次發布前後必須以正式 HTTPS 執行：首頁、六項工具頁、FAQ、候補頁、privacy、terms、404、`robots.txt`、`sitemap.xml` 與 `llms.txt` 的 HTTP 狀態檢查。
- 每次發布後必須確認 canonical／OG／JSON-LD 的正式網域、候補 receiver 的成功 202/readback、錯誤拒絕、重複去重與無敏感回應；不得用本機 fake receiver 代替正式 readback。
- 監控只記錄路由、狀態碼、延遲、版本與 request ID 雜湊／遮罩，不記錄 Email、名單、訊息內容、授權碼或原始 payload。
- 若首頁、候補 readback、私有資料邊界或安全標頭任一項失敗，停止推廣並回到上一個已驗證版本；回滾後重新跑同一組路由、候補與 crawler Gate。

## 六、交接結論

本候選版已達到「功能測試可交使用者驗收」；本輪完成次要 CTA、404 共用 shell、減少動態效果與手機導覽的 UX 補強。正式來源已確認，但尚未達到「可直接公開發布」。使用者驗收、公開事實、隱私／候補規則、外部 staging 驗收與發布授權完成前，維持 `FUNCTIONAL_TEST_READY_NOT_DEPLOYED`。本次新功能與文件更新尚未推送；GitHub 分支既有內容仍是候選源碼，不是正式公開網址。

## 七、2026-07-13 主窗口接續決策

使用者已完成購買正式網域：`dealalliancehub.com`。此紀錄只表示網域所有權已由使用者完成購買；本候選線尚未修改 Cloudflare DNS、GitHub Pages 自訂網域、Cloudflare Worker、secrets 或正式部署。

### 正式網址分工（規劃值，尚未切換）

| 用途 | 規劃網址 | 邊界 |
|---|---|---|
| 官方根網址 | `https://dealalliancehub.com` | 301/308 轉至 `www`，不直接承載兩份內容 |
| 公開官方網站 | `https://www.dealalliancehub.com` | 首頁、about、solutions、tools、resources、FAQ、隱私、條款、404 |
| 學生／會員入口 | `https://app.dealalliancehub.com` | 登入後的學生功能；不放公開網站導覽，直到正式授權與資料邊界驗收完成 |
| 管理員入口 | `https://admin.dealalliancehub.com` | 管理員專用，應由 Cloudflare Access 或等級相當的後台保護 |
| API | `https://api.dealalliancehub.com` | 網站、會員入口與工具的正式 API；不得放 secrets 或真實資料於前端 |
| 授權／下載 | `https://download.dealalliancehub.com` | 只回傳已授權的短效下載，不提供公開固定檔案網址 |
| 狀態頁 | `https://status.dealalliancehub.com` | 公開服務狀態與維護公告，不暴露內部 log 或客戶資料 |
| 隔離測試 | `https://staging.dealalliancehub.com` | 只給去識別化測試與主窗口驗收，不能作正式學生入口 |

### 接續規則

1. SEO／GEO 的 canonical、sitemap、OG、JSON-LD 與 `llms.txt` 已依主窗口確認，統一使用 `https://www.dealalliancehub.com`；部署後仍需做公開 readback。
2. `app`、`admin`、`api`、`download` 不得直接併入公開首頁的內容索引；每個入口要有自己的授權與監控 Gate。
3. 已有的 `*.workers.dev` staging URL 仍視為測試證據，不得直接當正式網址。
4. 本文件是主窗口接續依據；不需要透過聊天另外通知。若主窗口要執行外部動作，必須重新確認 DNS、部署、secrets、付款與正式發布授權。

### 尚未完成

- [ ] 由獲授權主窗口設定 DNS／Pages 自訂網域／Worker routes。
- [ ] 由獲授權主窗口確認正式 receiver、Origin 白名單與候補 202/readback。
- [x] 更新 `site.config.json` 與全站正式 canonical／sitemap／schema（本機候選已完成）。
- [x] 本機切換公開來源為 `https://www.dealalliancehub.com`，並完成 canonical／sitemap／OG／JSON-LD／`llms.txt` Gate。
- [ ] 完成公開 HTTPS、SEO/GEO 外部 readback、監控與回滾證據。
- [ ] 使用者完成公開網站瀏覽與核心流程驗收後，才可進入正式發布 Gate。

## 八、2026-07-13 正式部署實際紀錄（主窗口）

> 此段僅記錄已發生且已驗證的外部動作；不把 DNS 初始化或本機測試誤寫成正式網址上線。

- GitHub 正式發布分支：`agent/official-site-v1`，最新部署提交：`f681f47a393bb1fc986104223b3453f5bfec7905`。
- Cloudflare Pages 專案：`deal-alliance-official-site`；其預設 HTTPS 網址已回應 `200`：`https://deal-alliance-official-site.pages.dev`。
- 根網域規則已建立並啟用：`https://dealalliancehub.com/*` 以 `301` 導向 `https://www.dealalliancehub.com/${1}`；公開讀回已取得根網域的 `301 Location`。
- `www.dealalliancehub.com` 已送進 Pages 自訂網域流程，但截至本次檢查，Cloudflare 公開 DNS 尚未回傳 `www` 的 CNAME／A 記錄；因此 `https://www.dealalliancehub.com` 尚不可解析，`tests/verify_public_release.py` 正確回報 `FAIL_PUBLIC_RELEASE root_unreachable`。
- 候補表單維持 `disabled`；未接正式 receiver、未寫入真實名單、未建立付款／登入／下載宣稱。
- 已建立主窗口 DNS／HTTPS 監看 `dns-https`：每 5 分鐘只讀檢查；待 `www` 可解析且 HTTPS 生效後，會自動執行正式公開 Gate。若 DNS 仍未出現，維持未正式上線狀態，不宣稱完成。

### 2026-07-13 發布 Gate 關閉紀錄

- Cloudflare 公開 DNS 已解析 `www.dealalliancehub.com`，正式 HTTPS 首頁已回應 `200`。
- 正式 Gate 已通過：`PASS_PUBLIC_RELEASE origin=https://www.dealalliancehub.com paths=19 root_redirect=www crawler_assets=ok waitlist_post=not_performed resolver=public_doh+system_dns`。
- `www` DNS 已生效後，前一段的 `root_unreachable` 是本機 Tailscale DNS 快取，不是公開 DNS 故障；驗收器現在會在系統 DNS 失敗時以公開 DNS-over-HTTPS 做同一組只讀 GET 驗證。
- 公開官網階段可標示為 `FORMAL_PUBLIC_SITE_VERIFIED`；候補 receiver、登入、付款、下載、學生後台與工具授權維持未啟用，不納入本次發布完成宣稱。

## 2026-07-13 下一個唯一目標

讓訪客從正式官網找到正確的註冊／登入入口，安全進入後台，完成註冊後只看到自己被授權的服務。官網不保存密碼、session、token、學生資料或帳務資料。

主窗口必須先提供：核准的後台註冊 HTTPS URL、登入／Email 驗證／忘記密碼導向、最小欄位與同意版本、保存／刪除政策、測試 channel 與假帳號 E2E readback。未提供前，不做假註冊表單、不猜 API、不把候補頁冒充註冊。

技術 SEO/GEO 已通過正式公開 readback；Search Console、Bing 收錄、AI 實際引用、Core Web Vitals 與轉換數據仍屬上線後持續監測，不是一次 Gate 可永久宣稱完成。

詳細交界規格：[官網註冊入口整合契約_2026-07-13.md](./官網註冊入口整合契約_2026-07-13.md)。

## 2026-07-13 註冊入口候選交接

官網已先完成可維護的安全入口元件：`assets/site-config.js` 中的 `accountPortalRegisterUrl`、`accountPortalLoginUrl`、`accountPortalAllowedOrigins`、`accountPortalMode` 全部預設為空白／`disabled`。因此目前公開網站不會顯示或導向任何未驗證的註冊頁，也不會接收帳密或帳號資料。

主窗口完成後台的假帳號流程後，只需提供：已讀回的 HTTPS 註冊／登入網址、其唯一 Origin、登入／驗證／忘記密碼／取消的路由契約，以及去識別化 E2E 證據。官網線再啟用純導向 CTA 並重跑公開站＋跨系統測試；未通過前不得把它改為公開註冊完成。

啟用前須先跑：`<bundled-node> tests/verify_account_portal_runtime.mjs`。此驗收以 `example.invalid` 假網址執行同一份官網 JS，確認 disabled、缺一網址、錯 Origin 或 HTTP 都不會渲染入口；只有兩個核准 HTTPS 假網址才會產生兩個純導向連結。

所有公開頁與 404 也已固定先載入 `site-config.js`、再載入 `site.js`。公開 readback Gate 會檢查此順序，避免只在部分頁面出現帳號入口。

## 2026-07-13 最新部署同步 Gate（取代本文件任何較早的同步完成敘述）

- 現行候選 `dist/` 已包含 `/assets/site-config.js`，且 15 個公開頁與 `404.html` 都在 `/assets/site.js` 前載入它。
- 本機 Gate 已通過：`PASS_ACCOUNT_PORTAL_BOOTSTRAP pages=16 artifacts=source+dist`。
- 對 `https://www.dealalliancehub.com` 的唯讀公開 Gate 最新結果：`FAIL_PUBLIC_RELEASE path=/ account_portal_bootstrap_missing`。
- 主窗口下一步唯一外部動作：在既有授權流程中，受控發布現行 `200_官網整合/公開官網候選_v1/dist/` artifact，並保持 `assets/site-config.js` 中候補與帳號入口均為 `disabled`／空 URL。
- 發布後由官網線重新執行 `python3 tests/verify_public_release.py`；只有取得 `PASS_PUBLIC_RELEASE` 才能將候選同步狀態改回已驗證。
- 本次沒有推送、部署、修改 Cloudflare／DNS／Pages／Worker、啟用 receiver、送出表單，或讀取任何真實資料。

## 2026-07-14 受控發布前的 V3 狀態快照更新

本次只更新候選來源與 `dist/`，沒有推送或部署。

1. 依權威工具表 SHA-256 `2485b6d04c82812cce4e17f48d1bb156ac93ea34fbcf186bc52aad3d1f9d87d8`，將 LINE 公開候補事實更新為 `MAC_COMPILED_V3_CANDIDATE_PRE_SIGN_GUI_PENDING`。
2. 公開目錄維持保守：`WAITLIST_ONLY`、不可下載；不公開內部 marker、hash、簽章材料或流程細節。
3. `bash scripts/build-public.sh` 已從最新來源重建 `dist/`。完整本機 Gate PASS，包括 `PASS_TOOL_SOURCE_ALIGNMENT products=5 source=400_status_table+release_snapshot`、`PASS_ACCOUNT_PORTAL_BOOTSTRAP pages=16 artifacts=source+dist` 與 `PASS_ACCOUNT_PORTAL_RUNTIME ... fake_urls_only=true`。
4. 下一次官網發布只能取最新 `dist/`；`site-config.js` 的帳號入口與候補 receiver 仍必須保持空白／`disabled`。發布後先跑 `python3 tests/verify_public_release.py`，再決定是否更新公開已驗證狀態。

## 2026-07-14 發布完成與網站邊界確認

- 發布 commit：`94e0170`。GitHub Pages workflow run #2 的 validate 與 deploy 均成功；首次 run #1 因 Pages 未啟用而停止，已作為設定缺口紀錄，不算成功。
- Cloudflare Pages 部署清單已讀到相同提交；公開 HTTPS Gate 已回傳 `PASS_PUBLIC_RELEASE`（19 路由、root → www、crawler assets、無候補 POST）。
- 正式公開網址維持：`https://www.dealalliancehub.com`。GitHub Pages 的預設網址只作為發布平台證據，不是對外 canonical。
- 本次未啟用帳號入口、候補 receiver、付款、下載、工具發送或資料收集。下一個跨系統 Gate 仍是後台隔離 staging HTTPS 的去識別化登入／權益 E2E。

## 2026-07-14 LINE V4 候補同步待發布

- LINE 最新權威 marker 已由 V3 提升為 `MAC_COMPILED_V4_CANDIDATE_PRE_SIGN_GUI_PENDING`；公開仍一律是 `WAITLIST_ONLY`／不可下載。
- 本次變更只同步候選來源與內部狀態快照，須重建 `dist/`、在乾淨發布工作樹通過 Gate，才可進入下一次受控發布。不得把 V4 的 fake／typecheck 證據說成 GUI、真實 LINE 或正式 App 發放完成。
