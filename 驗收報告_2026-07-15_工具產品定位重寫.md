# 工具產品定位重寫驗收（候選官網）

階段：`PENDING_USER_ACCEPTANCE`  
範圍：僅 `200_官網整合/公開官網候選_v1/`；不部署、不推送、不接收資料。

## 本輪決策

成交聯盟是服務銷售工作者與團隊的**工作流程工具平台**，不是課程或學習路徑網站。

- 訪客頁先說工作與生活的利益：減少重複、遺漏與不安，把時間留回客戶、健康、家人與生活。
- 再說已驗證的能力：名單整理、聯繫前預覽／確認、訊息流程檢查、建檔與後續行動整理。
- MBAF 只作為內部寫作方法；訪客頁不出現 MBAF、課程、學習路徑、成交地圖、DISC 或 Top Sales。
- 四項公開工具仍全部 `WAITLIST_ONLY`；不宣稱可下載、付款、試用、真實發送或處理客戶資料。

## 已套用頁面

- 首頁、使用情境、工具總覽、四項工具說明、產品原則、FAQ、關於我們、開放狀態、隱私、條款、404 與搜尋頁。
- `llms.txt`、JSON-LD、canonical／OG、公開讀回 Gate 與 release contract 同步改為 `PRODUCT_PLATFORM_20260715`。
- 先前學生／課程方向的三份本機審稿／驗收檔已標示「已停用」，避免被誤用。

## 去識別端到端驗收

2026-07-15 本機 build 與去識別驗收全數通過：

- `PASS_ALL_LOCAL_GATES`：18 個本機公開路徑、結構化資料、canonical／sitemap、`llms.txt`、行動版與導覽流程均通過。
- `PASS_PUBLIC_BOUNDARY`：候補頁無表單／receiver；四項公開工具仍為候補；生涯運數計算器與跟進節奏工具未被建置到公開輸出。
- `PASS_VISITOR_NAVIGATION_E2E`：4 條訪客導覽流程完成；沒有表單提交與未開放工具的互動示範。
- `PASS_PUBLIC_CLAIM_SCAN`：公開頁無保證成交、下載、付款、試用或未授權功能宣稱。
- `PASS_SOURCE_BACKED_PRODUCT_COPY`：產品定位、四項工具與安全邊界均對齊來源台帳。
- `PASS_RELEASE_CONTRACT`、`PASS_GOAL_READINESS_AUDIT`、`PASS_WEBSITE_PRODUCT_OPERATIONS_CANDIDATE`：發布契約、帳號 CTA 邊界、SEO／GEO 人本可理解原則與營運限制均保持一致。
- `PASS_ACCOUNT_PORTAL_PUBLIC`：只讀 GET 驗證 `https://app.dealalliancehub.com/register`、`/login` 均可開啟，具 `no-store`、`noindex`、`nosniff`、`no-referrer`；credential form 僅在 app 同源頁，不存在於公開官網。

驗收未提交表單、建立帳號、傳遞帳密／token，或接觸真實客戶資料。

## 本機瀏覽器驗收

- 預覽必須使用 `http://127.0.0.1:4173/`，不能直接開 `file://`；後者無法正確載入根路徑的 CSS 與帳號 CTA 腳本。
- 實際瀏覽器讀回首頁：主標、六個產品導覽項目、兩個帳號 CTA、四個工作情境、產品原則與頁尾均正常呈現。
- 桌機 `1280×720` 與手機 `390×844` 檢查均無水平溢位；樣式表已載入，帳號 CTA 僅為 `https://app.dealalliancehub.com/login` 與 `/register` 的純連結。

## 正式公開讀回

2026-07-15 最新 GET-only 執行 `python3 tests/verify_public_release.py` 回傳：

`FAIL_PUBLIC_RELEASE hidden_path=/tools/life-number-calculator/ status=200`

同一輪額外以 GET 分別確認，`/tools/follow-up-rhythm/` 也回傳 HTTP `200`。

結論：正式網域仍保留兩條已要求隱藏的工具路徑：生涯運數計算器與跟進節奏；候選 artifact 已排除兩者，正式來源尚未與候選同步。這是正確的發布阻擋，不是候選站失敗；主窗口受控發布目前 `dist/` 時必須一併移除兩條舊路徑，再以相同的 GET-only Gate 重讀所有公開路徑與本輪產品定位內容。

## 尚未完成與風險

- 正式網域仍公開舊的生涯運數與跟進節奏路徑；本輪 `PRODUCT_PLATFORM_20260715` 候選更新尚未完成受控發布與正式公開讀回。
- 公開官網只提供 app 註冊／登入的安全純連結；app 表單已可開啟，但 Email 驗證、試用權益與完整 lifecycle 的公開讀回尚未完成。付款、下載、receiver 與工具執行仍未開放。
- 營運正式就緒仍受 release contract 記錄的 7 項 owner inputs 約束。

## 未執行外部動作與回滾

- 未 push、未部署、未改 Cloudflare、未改 DNS／Pages／Worker、未送出任何表單或通知。
- 回滾：在候選分支回退本輪頁面、`llms.txt`、內容台帳、release contract 與 Gate 變更，再重跑同一組本機 Gate；正式站由主窗口依既有受控發布流程回退前一個 artifact。

## 今天學到什麼與防呆

- **使用者修正：** 成交聯盟是工具產品平台；MBAF 只能作內部文案順序，不能被公開訪客誤看成課程或學習路徑。
- **根因：** 早期候選的課程導向審稿、交接與維護 Skill 沒有在產品定位改變後一併標示淘汰，README 也沒有阻止直接用 `file://` 開站。
- **固定防呆：** 未來每次改定位，必須同時掃描公開 HTML、SEO／GEO、`llms.txt`、README、發布交接、驗收報告與網站維護 Skill；過期文件保留歷史證據但要在第一屏標「已停用」。本機視覺驗收必須經 HTTP preview，不可用 `file://`。
- **建議回寫位置：** 本候選的 `skills/orikan-website-product-operations-candidate/SKILL.md` 已更新；主窗口應在核准後把同一條「工具產品公開定位／HTTP preview」防呆升格到 TaixinMemoryHub 的正式網站維護 Skill。

## 2026-07-15 正式網址收尾讀回（取代本報告中較早的公開阻擋結論）

- Cloudflare Pages 自訂網域：`www.dealalliancehub.com` 已顯示 **Active／SSL enabled**，且綁定 `deal-alliance-official-site.pages.dev`。
- 公開 DNS-over-HTTPS 讀回：`https://www.dealalliancehub.com/` 為 200；根網域正確導向 `www`；`/tools/life-number-calculator/` 與 `/tools/follow-up-rhythm/` 均為 404，並含 `Cache-Control: no-store` 與 `X-Robots-Tag: noindex, nofollow, noarchive`。
- 完整 Gate：`PASS_PUBLIC_RELEASE origin=https://www.dealalliancehub.com paths=17 hidden_paths=2 content_revision=PRODUCT_PLATFORM_20260715 root_redirect=www crawler_assets=ok account_portal_config=verified account_portal_routes=2 private_headers=ok credential_forms=app_only_same_origin waitlist_post=not_performed resolver=public_doh+system_dns`。
- 驗收器修正：Cloudflare Pages Function 回傳的正常 404 與靜態 404 都視為退役頁成功移除；先前把前者判失敗是驗收器缺陷，不是網站缺陷。
- 邊界不變：付款、正式下載、候補 receiver、工具實際執行，以及 Email 驗證／7 天試用完整生命週期尚未開放或完成遠端驗證；`operational_release_ready=false`，7 項公開法務／客服資料仍待 owner 補齊。
- 回滾：Cloudflare Pages 回退到前一 verified deployment 後，必須再跑 `tests/verify_public_release.py`；不得把快取命中當作成功證據。
