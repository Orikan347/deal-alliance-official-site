# 成交聯盟官方網站｜公開監控與回滾演練清單

目前技術狀態：`PUBLIC_TECHNICAL_RELEASE_VERIFIED`  
營運狀態：`operational_release_ready=false`  
適用範圍：`https://www.dealalliancehub.com` 與已核准的帳號入口；不提交表單、不登入、不讀取任何客戶／學生資料。

## 監控要回答的事

不是只確認首頁可開，而是確認訪客不會被導向錯站、錯帳號入口或未授權服務。

| 檢查 | 方式 | 成功條件 | 不可記錄的資料 |
|---|---|---|---|
| 根網域 | HTTPS GET | `dealalliancehub.com` 最終到 `www` | cookie、帳密、query token |
| 公開路由 | `verify_public_release.py` | release contract 的每條公開路由皆 200、canonical／OG／schema 正確 | 表單內容、訪客 IP、可識別資料 |
| 帳號 CTA config | 同一公開 Gate | 只允許已核准的 `/register`、`/login` 和單一 origin | password、session、reset token |
| 帳號私有頁 | `verify_account_portal_public.py` 的 GET-only | 200、`no-store`／`noindex`、無 credential form | 帳號、登入嘗試、OAuth token |
| 候補邊界 | 公開 Gate＋本機 fake E2E | receiver 仍 disabled；不做 POST | Email、候補名單、請求 payload |

## Owner／主窗口待完成的外部設定

以下由有權限的主窗口在 Cloudflare／既有監控系統設定；本官網線不執行。

- [ ] 指定一位接收網站異常通知的人：`＿＿＿＿＿＿＿＿`
- [ ] 建立首頁與根網域轉址的 HTTPS GET-only 監控。
- [ ] 建立 release contract 宣告的公開路由之定期 readback 或既有外部監控。
- [ ] 建立帳號入口兩條 GET-only 私有標頭檢查；不得自動填表、登入或建立帳號。
- [ ] 設定告警只保存 route、HTTP status、latency、版本與遮罩 request ID；不保存 payload、password、session、token、Email 或姓名。
- [ ] 為故障通知指定回覆時限與處理責任人：`＿＿＿＿＿＿＿＿`

## 建議驗收指令（只讀）

```bash
python3 tests/verify_public_release.py
python3 tests/verify_account_portal_public.py --resolve-ip <公開 DNS IPv4>
```

兩者都不會提交候補表單、建立帳號、登入或傳遞帳密。

## 回滾觸發條件

任一條成立時，暫停公開變更並由主窗口回滾：

1. 任一公開路由無法取得 200，或根網域不再導向 `www`。
2. canonical、OG、sitemap 或 `llms.txt` 回到舊網域／錯誤內容。
3. `assets/site-config.js` 不是核准的 account CTA 設定。
4. 帳號私有頁缺少 `no-store`／`noindex`，或出現 credential form。
5. 候補 receiver、付款、下載或未授權工具被意外開啟。
6. 公開頁可能收集、顯示或記錄真實帳密、token、學生／客戶或付款資料。

## 回滾步驟（只由主窗口執行）

1. 在 Cloudflare Pages 回復到上一個已驗證 artifact／commit（目前 contract 記錄前一 release 為 `38ad43a`）。
2. 以正式網址重跑兩個 GET-only Gate。
3. 確認四項來源可追溯工具仍是 `WAITLIST_ONLY`、生涯運數與跟進節奏工具未進公開 artifact、候補 receiver 仍 disabled，且帳號 CTA 回到已驗證安全狀態。
4. 在此文件記錄時間、觸發條件、回滾版本、公開 Gate 結果；**不要**記錄任何敏感資料。
5. 修正候選來源、以假資料跑完整 Gate，才可再次受控發布。

## 演練完成紀錄

- 演練日期：`＿＿＿＿＿＿＿＿`
- 演練者：`＿＿＿＿＿＿＿＿`
- 模擬觸發：`＿＿＿＿＿＿＿＿`
- 回滾 artifact／commit：`＿＿＿＿＿＿＿＿`
- 正式 GET-only Gate 結果：`PASS／FAIL`
- 是否確認未收集／未記錄敏感資料：`是／否`
