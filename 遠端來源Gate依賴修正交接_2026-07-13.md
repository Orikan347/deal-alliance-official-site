# 成交聯盟官方網站｜遠端來源 Gate 依賴修正交接

日期：2026-07-13  
目前階段：`PENDING_RELEASE_SECURITY`  
狀態：`CANDIDATE_GATE_FIXED_AND_VERIFIED_NOT_DEPLOYED`  
範圍：僅 `200_官網整合/公開官網候選_v1/` 與其專屬驗收檔案。

## 本次問題與根因

遠端部署 repo 同步候選後執行 `tests/verify_site.py`，曾失敗：

```text
tools: source status table is missing
```

根因不是工具狀態不存在，而是驗收器把平台工作區外的權威檔案 `400_桌面程式優化/工具整合狀態表.md` 當成必有依賴。隔離的部署 repo 依設計不會包含這份跨產品檔案。舊作法要靠 `DEAL_ALLIANCE_RELEASE_ISOLATED=1` 才改用目錄標籤，容易因執行入口漏設環境變數而失敗，且只驗證標籤的證據強度不足。

## 最小修正

1. 新增 `status_evidence/tool_status_snapshot.json`：記錄五項受平台狀態表管理的工具狀態碼、對外保守狀態、擷取日與權威表 SHA-256。
2. `tests/verify_site.py` 改為永遠要求此快照；不再依賴環境變數。
3. 本機有權威表時，Gate 必須同時驗證：權威表 SHA-256、五個狀態碼、快照、公開 `tools/catalog.json` 的狀態／日期完全一致。
4. 隔離部署 repo 沒有權威表時，Gate 仍驗證同一份版本化快照與公開目錄，輸出 `source=release_status_snapshot`；不是跳過檢查。
5. Gate 額外拒絕 `dist/status_evidence`，確保內部證據不進公開 artifact。
6. Pages workflow 已移除不再需要的 `DEAL_ALLIANCE_RELEASE_ISOLATED` 環境變數。

## 修改檔案

- `tests/verify_site.py`：工具狀態的來源依賴、SHA-256、公開目錄一致性與公開 artifact 邊界 Gate。
- `status_evidence/tool_status_snapshot.json`：由權威狀態表擷取的可追溯 release evidence；不會被 `scripts/build-public.sh` 複製到 `dist/`。
- `.github/workflows/pages-manual-release.yml`：移除會掩蓋缺少依賴的環境變數。
- 本文件：本次問題、測試、風險與今日規則學習。

## 假資料／端到端驗證證據

所有測試均未讀取真實客戶、學生、帳號或付款資料，候補流程只使用 `example.invalid` 假資料。

### 1. 有權威狀態表的完整 Gate

```text
PASS_ACCOUNT_PORTAL_RUNTIME ... fake_urls_only=true
PASS_WAITLIST_FAKE_E2E status=202 readback=request_id dedupe=stable reject=400 fake_payload_only default_mode=disabled
PASS_TOOL_SOURCE_ALIGNMENT products=5 source=400_status_table+release_snapshot
PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com public_release_verified=true registration_pending=true
```

### 2. 隔離部署 repo 模擬

將現有候選原樣複製到 `/private/tmp` 隔離資料夾；該位置沒有 `400_桌面程式優化/工具整合狀態表.md`，且不設定任何 `DEAL_ALLIANCE_RELEASE_ISOLATED` 環境變數。完整 Gate 通過：

```text
PASS_TOOL_SOURCE_ALIGNMENT products=5 source=release_status_snapshot
PASS_ACCOUNT_PORTAL_BOOTSTRAP pages=16 artifacts=source+dist
PASS_LOCAL_HTTP routes=20
PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com public_release_verified=true registration_pending=true
```

### 3. 發布 artifact 邊界

執行 `bash scripts/build-public.sh` 後，`dist/status_evidence` 不存在；再跑 Gate 通過，證明內部快照沒有被公開。

## 今日學習結論（建議主窗口回寫共用規則）

### 今天修正了什麼

修正「部署 repo 的 Gate 不能找到平台工作區外狀態表」；改成部署來源自帶、可追溯、可與權威表核對、且不會公開的 evidence snapshot。

### 以後的防呆規則

1. 可部署 repo 的每一個 release Gate 都必須在乾淨隔離 checkout 直接可重跑；不得依賴同機其他專案路徑、未紀錄環境變數或聊天記憶。
2. 若公開文案需要引用跨產品真相，發布來源必須攜帶版本化且可追溯的最小 evidence snapshot；快照要有來源路徑、日期、雜湊與實際狀態碼。
3. 本機有權威來源時必須驗證 snapshot 未過期；雜湊不符即 fail，先更新快照與公開目錄，再談發布。
4. evidence snapshot 是驗收依據，不是公開內容；build script 必須白名單複製公開檔，Gate 必須確認 evidence 沒進 `dist/`。
5. 不可用環境變數把「缺依賴」變成 pass；環境差異只能被明確測試，不可成為降低驗收的捷徑。

### 建議回寫位置（本聊天室未修改）

- 首選：`03_工作檔/skills/deal-alliance-product-delivery/SKILL.md` 的「網站與後台規格／公開網站」新增「部署 repo Gate 獨立性與 evidence snapshot」規則。
- 同步建議：`TaixinMemoryHub/references/software-security-release-policy.md` 的「每次更新必留紀錄」新增「跨工作線事實的可追溯 release snapshot」規則。
- 原因：這是所有工具、網站與後台都會重複遇到的交付安全問題，不該只留在本次官網驗收器。

## 未完成風險

- 快照代表 2026-07-13 的權威表；工具狀態一有更動，SHA-256 Gate 會正確阻擋本機發布，必須由擁有平台狀態表的主窗口更新快照與公開狀態後再發布。
- 正式公開網站仍尚未同步最新 `site-config.js` bootstrap；本次僅修正候選／部署來源 Gate，沒有部署。
- 後台 staging HTTPS、假帳號註冊／登入跨系統 E2E、真實候補 receiver、付款、工具授權與使用者驗收仍未完成，不得因本 Gate 通過宣稱整體網站正式完成。

## 未執行外部動作

未 push、未部署、未修改 GitHub 遠端、Cloudflare、DNS、Pages、Worker、付款、secrets 或 `l2_current_truth.json`；未送出表單、未建立外部帳號，未讀取真實客戶／學生資料。

## 回滾

本次只在官網候選內新增快照並調整驗收器／workflow。若需回滾，還原 `tests/verify_site.py`、刪除 `status_evidence/tool_status_snapshot.json`、還原 workflow 的單一步驟即可；不影響已上線網站、後台或 Cloudflare。回滾後隔離部署 repo 會再次缺少證據，故只能作緊急診斷，不應作正式發布路徑。

## 主窗口下一步

將此三檔同步到部署 repo 後，在乾淨 checkout 直接執行 `python tests/verify_site.py`（不設環境變數）；預期應見 `PASS_TOOL_SOURCE_ALIGNMENT products=5 source=release_status_snapshot`。通過後才可進行既有受控發布流程；本聊天室不執行推送或部署。

## 2026-07-13 權威工具狀態校正（以本節為最新狀態）

主窗口更新權威 `400_桌面程式優化/工具整合狀態表.md` 後，發現舊 snapshot 將簡訊 r27 與 LINE 誤寫為較早的「已簽章候選」。這會誤導公開候補狀態，因此已立即校正。

### 已校正的權威狀態

| product_id | 最新權威狀態碼 | 公開狀態 |
|---|---|---|
| `sms_suite` | `R27_CORE_PRODUCT_REVIEW_USER_ACCEPTED_STAGING_OAUTH_PENDING` | `WAITLIST_ONLY`；候補中、正式發放未開放 |
| `line_automation` | `MAC_PRIVATE_NOTARIZED_DMG_DELIVERY_BUNDLE_FAIL_PENDING_COMPILED_RUNTIME` | `WAITLIST_ONLY`；候補中、不可下載、交付包修復中 |
| `contact_converter` | `LOCAL_FAKE_E2E_PASS_USER_VISIBLE_ACCEPTANCE_AND_STAGING_PENDING` | `WAITLIST_ONLY`；候補中、正式服務待確認 |
| `smart_close` | `LOCAL_USER_ACCEPTED_STAGING_RELEASE_PENDING` | `WAITLIST_ONLY`；候補中、正式 AI Gate 待確認 |
| `life_number_calculator` | `RESERVED_NOT_OPEN` | `NOT_ENABLED`；預留中、尚未開放 |

### 本次修改

- 更新 `status_evidence/tool_status_snapshot.json`：來源 SHA-256 改為 `487f66947b4beb3b8f6e2e60abb4cc076e51af62264582aa80989f67ae2cd380`，五項狀態碼與公開摘要／平台說明均對齊權威表。
- 更新 `tests/verify_site.py`：預期狀態碼更新，並強制驗證 catalog 的 `offer_status`、`status_label`、`summary`、`platform` 與 snapshot 一致。
- 更新 `tools/catalog.json` 與簡訊／LINE 詳情頁：公開文案保持候補／未開放，不提供下載、登入或發送；不公開內部狀態碼。
- 重建 `dist/`；內部 `status_evidence/` 仍未被放入公開 artifact。

### 驗證結果

權威表存在時完整 Gate：

```text
PASS_TOOL_SOURCE_ALIGNMENT products=5 source=400_status_table+release_snapshot
PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com public_release_verified=true registration_pending=true
```

隔離 checkout（沒有權威表、無環境變數）Gate：

```text
PASS_TOOL_SOURCE_ALIGNMENT products=5 source=release_status_snapshot
PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com public_release_verified=true registration_pending=true
```

未 push、未部署、未修改 Cloudflare／DNS／Pages／Worker、未送表單、未讀取真實資料。回滾方式是還原本節列出的候選檔並重建 `dist/`；不影響正式網站或平台權威表。

## 2026-07-13 LINE V2 最終事實對齊（以本節取代上一節 LINE 狀態）

權威表在上一輪後再次更新。LINE 的唯一正確 marker 為：

```text
MAC_COMPILED_V2_CANDIDATE_PRE_SIGN_GUI_PENDING
```

它只代表編譯式 native V2 已通過交付掃描與去識別化 fake/no-send Gate；GUI 開啟、LINE driver 的不發送實機驗收、重簽與公證都尚未完成。舊私有 notarized DMG 因 `MAC-PACKAGE-001` 仍不可下載，不能被當成 V2 可交付證據。

本次最終修改：

- `status_evidence/tool_status_snapshot.json`：LINE marker 更新為上述值；權威表 SHA-256 更新為 `efa7f3fa5fe1b18e98c9127b57c58330678f67134b0a7b94bf4b2b1ecb89c363`。
- `tests/verify_site.py`：LINE 預期 marker 同步更新。
- `tools/catalog.json` 與 `tools/line-automation/index.html`：維持 `WAITLIST_ONLY`，公開只寫「候補中・不可下載，V2 實機驗收中」與 GUI／不發送／重簽／公證待完成；未加入下載、登入或發送入口。
- 已重建 `dist/`。

最終測試：

```text
PASS_TOOL_SOURCE_ALIGNMENT products=5 source=400_status_table+release_snapshot
PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com public_release_verified=true registration_pending=true
PASS_TOOL_SOURCE_ALIGNMENT products=5 source=release_status_snapshot
```

第二個 `PASS` 是實際複製至隔離 checkout（沒有權威狀態表、沒有環境變數）後取得。`rg` 查核確認舊 `MAC_PRIVATE_NOTARIZED_DMG_DELIVERY_BUNDLE_FAIL_PENDING_COMPILED_RUNTIME` marker 在候選的 `tests/`、`status_evidence/` 與 `tools/` 中沒有殘留。

## 2026-07-14 發布 workflow 真順序防呆

主窗口獨立 preflight 發現並修正 workflow 順序問題，本官網線已只讀確認，**沒有覆寫 workflow**。

正確順序固定為：

```text
verify_release_contract → verify_headers_contract → verify_waitlist_contract
→ build-public (重建 dist)
→ verify_site (讀取 source + dist)
→ verify_account_portal_runtime
→ Configure / Upload Pages artifact
```

原因：`tests/verify_site.py` 會驗證 `dist/llms.txt`、每個公開頁的 `site-config.js` bootstrap 與 `dist` 的內部證據排除規則。若在 `build-public.sh` 前執行，它可能驗到上一次留下的 `dist`，導致來源已更新卻被舊 artifact 產生假 PASS 或假 FAIL。

以後 workflow 不得把 `verify_site.py` 移回 build 前；凡是讀取 `dist/` 的 Gate 一律在公開 artifact 重建後才執行。這是部署 Gate 防呆，不是降低驗收。

本節只補記官網交接；未修改 `.github/workflows/pages-manual-release.yml`、未 push、未部署、未修改 Cloudflare／DNS／Pages／Worker，也未送出表單或讀取真實資料。回滾只需移除本節文字，對候選 artifact 與外部服務沒有影響。

## 2026-07-14 正式公開 readback 與後台入口預檢

### 已驗證：公開官網 artifact 已同步

以唯讀 `tests/verify_public_release.py` 驗證結果：

```text
PASS_PUBLIC_RELEASE origin=https://www.dealalliancehub.com paths=19 root_redirect=www crawler_assets=ok waitlist_post=not_performed resolver=public_doh+system_dns
```

這證明公開首頁、15 個 sitemap 頁、404、`robots.txt`、`sitemap.xml`、`llms.txt`、安全標頭、canonical、OG、JSON-LD 與根網域轉 `www` 已讀回通過。候補表單沒有被提交。

### 尚未驗證：後台註冊／登入 HTTPS

未登入、未送資料的 GET 預檢結果：

```text
https://app.dealalliancehub.com/register → status 000, DNS unresolved
https://app.dealalliancehub.com/login    → status 000, DNS unresolved
```

因此 `assets/site-config.js` 的帳號入口必須繼續維持 `disabled`／空 URL；不得因公開官網通過而提前顯示建立帳號或登入連結。這也不代表後台功能失敗，只表示 staging／app HTTPS、DNS 與跨系統 fake-account readback 尚未提供。

### 主窗口解除條件

1. 建立並讀回隔離後台的 HTTPS `app` origin、`/register`、`/login`。
2. 提供去識別化假帳號的註冊、驗證、取消、登入、忘記／重設密碼、登出與「我的工具」權益拒絕／允許證據。
3. 官網線才可把兩個已驗證 HTTPS URL 寫入設定，啟用純導向 CTA，並重新跑公開站＋跨系統 Gate。

本輪未修改外部服務、未建立帳號、未送表單、未讀取真實資料；回滾方式為保持或恢復 `accountPortalMode: "disabled"` 與空 URL，公開官網不受影響。

## 2026-07-14 LINE V3 來源事實與隔離發布 Gate 再驗證

### 更新原因

較早的候選快照仍記錄 LINE native V2。主窗口已獨立驗證權威工具表的最新 V3 事實，因此候選網站必須先同步，才可被下一次受控發布使用。

### 本次最小同步

- `status_evidence/tool_status_snapshot.json` 的權威 SHA-256 更新為 `2485b6d04c82812cce4e17f48d1bb156ac93ea34fbcf186bc52aad3d1f9d87d8`，LINE marker 更新為 `MAC_COMPILED_V3_CANDIDATE_PRE_SIGN_GUI_PENDING`。
- `tests/verify_site.py`、`tools/catalog.json` 與 `tools/line-automation/index.html` 同步；公開只維持「候補中・不可下載，V3 實機驗收中」。
- 重新執行 `bash scripts/build-public.sh`；`dist/` 不含 `status_evidence/`，內部狀態證據不會被公開。

### 實測證據

```text
PASS_RELEASE_CONTRACT paths=19 monitoring=6 public_release_verified=true registration_pending=true
PASS_HEADERS_CONTRACT security_headers=7 candidate_only=true
PASS_WAITLIST_FAKE_E2E status=202 readback=request_id dedupe=stable reject=400 fake_payload_only default_mode=disabled
PASS_TOOL_SOURCE_ALIGNMENT products=5 source=400_status_table+release_snapshot
PASS_LLMS_DISCOVERY public_origin=true routes=6 privacy_boundary=true
PASS_ACCOUNT_PORTAL_BOOTSTRAP pages=16 artifacts=source+dist
PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com public_release_verified=true registration_pending=true
PASS_ACCOUNT_PORTAL_RUNTIME disabled=hidden incomplete=hidden invalid_origin=hidden http=hidden approved_https=two_links fake_urls_only=true
```

### 今日防呆

每次候選站引用跨工具狀態時，必須同時核對「權威表 SHA、workflow 實際產製來源、snapshot、公開 catalog、重建後 dist」五個層次；任何舊名稱相近的副本都不能取代 workflow 實際來源。此規則已寫入主窗口更新紀錄，未在本次啟用任何外部服務。

### 外部邊界

未 push、未部署、未改 GitHub／Cloudflare／DNS／Pages／Worker，未啟用帳號入口或候補 receiver，未送表單、未讀真實資料。下一個外部動作仍是主窗口受控發布最新 `dist/` 後的 GET-only 公開讀回。

## 2026-07-14 權威工具表 V4 雜湊續期

權威 `400_桌面程式優化/工具整合狀態表.md` 在 V4 狀態之後仍有內容更新，導致候選快照保留的舊 SHA-256 與目前來源不一致。公開 catalog、LINE 詳情頁與驗收器已正確維持 V4，這次只更新證據快照的來源雜湊：

```text
6cbe99ec75a410b8123b2c769a23e25b7255b453ee8f49ebd76b9924dcefba97
```

LINE 的唯一正確來源 marker 仍是：

```text
MAC_COMPILED_V4_CANDIDATE_PRE_SIGN_GUI_PENDING
```

公開站仍只顯示 `WAITLIST_ONLY`／「候補中・不可下載，V4 實機驗收中」；沒有加入下載、帳號登入或訊息發送功能。此續期後必須重建 `dist/` 並重跑本機及隔離 checkout Gate，才可作為下次候選發布依據。

### 續期後驗證（假資料／唯讀）

```text
PASS_RELEASE_CONTRACT paths=19 monitoring=6 public_release_verified=true registration_pending=true
PASS_HEADERS_CONTRACT security_headers=7 candidate_only=true
PASS_WAITLIST_FAKE_E2E status=202 readback=request_id dedupe=stable reject=400 fake_payload_only default_mode=disabled
PASS_TOOL_SOURCE_ALIGNMENT products=5 source=400_status_table+release_snapshot
PASS_ALL_LOCAL_GATES candidate_origin=www.dealalliancehub.com public_release_verified=true registration_pending=true
PASS_ACCOUNT_PORTAL_RUNTIME disabled=hidden incomplete=hidden invalid_origin=hidden http=hidden approved_https=two_links fake_urls_only=true
PASS_TOOL_SOURCE_ALIGNMENT products=5 source=release_status_snapshot
PASS_PUBLIC_RELEASE origin=https://www.dealalliancehub.com paths=19 root_redirect=www crawler_assets=ok waitlist_post=not_performed resolver=public_doh+system_dns
```

隔離副本不含平台權威表，仍可由內建 snapshot 通過 Gate；假候補流程只使用 `example.invalid` 與本機 fake receiver，未持久化資料。公開讀回只做 GET，未送候補表單。

### 後台入口仍為外部阻塞

2026-07-14 唯讀 HTTPS 預檢：

```text
https://app.dealalliancehub.com/register → status 000, DNS unresolved
https://app.dealalliancehub.com/login    → status 000, DNS unresolved
```

所以帳號 CTA 必須繼續 fail-closed（`accountPortalMode: "disabled"`、兩個 URL 為空）。這不是公開站的 DNS 問題；`www` 已通過公開讀回。主窗口完成 app HTTPS 與去識別化假帳號生命周期證據前，官網線不得啟用建立帳號／登入連結。

### 今天學到什麼（本次續期）

- 問題：快照中的 LINE marker 已是 V4，但權威表只要另有任何文字更新，舊 SHA 仍會使候選 Gate 正確失敗。
- 防呆：每次收到工具狀態更新，必須先對比「marker、公開 catalog、來源 SHA」三者；只改公開文案或只改 marker 都不足以代表可發布。
- 建議共用回寫：維持寫入 `deal-alliance-product-delivery/SKILL.md` 的「版本化 evidence snapshot」規則，並將「來源雜湊更新也必須重跑隔離 checkout」列為必要 Gate。

### 本次未執行外部動作與回滾

未 push、未部署、未修改 GitHub、Cloudflare、DNS、Pages、Worker、付款、secrets 或 `l2_current_truth.json`；未建立帳號、未登入、未送表單、未讀取真實客戶／學生資料。回滾只需將 `status_evidence/tool_status_snapshot.json` 的 SHA 還原到前一版並移除本節文字，但那會使目前權威表的 Gate 故意失敗；不會影響已公開網站或任何外部服務。
