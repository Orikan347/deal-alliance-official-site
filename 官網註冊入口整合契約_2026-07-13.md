# 成交聯盟官方網站｜註冊入口整合契約

版本：`v1.1`
目前階段：`PENDING_USER_ACCEPTANCE`
適用範圍：只規範公開官網與後台註冊入口的交界，不修改後台程式、資料庫、Cloudflare、secrets 或正式帳號資料。

## 唯一最終目標

讓訪客從 `https://www.dealalliancehub.com` 清楚找到「註冊／登入」入口，並安全地轉交至核准的學生／會員後台；完成註冊後，後台能建立帳號、驗證身分、套用權限，再讓使用者進入自己的服務。公開官網不保存密碼、不建立 session、不接觸學生資料，也不自行假造註冊成功。

## 目前已確認

- 正式公開網址已只讀驗證：`PASS_PUBLIC_RELEASE origin=https://www.dealalliancehub.com paths=19 root_redirect=www crawler_assets=ok waitlist_post=not_performed`。
- 公開頁的技術 SEO/GEO 已完成：canonical、title、description、OG、JSON-LD、sitemap、robots、`llms.txt`、手機 viewport、公開安全標頭與內部連結均已驗證。
- 這不是「SEO 已保證完成」：Search Console、Bing 收錄、實際 AI 引用、Core Web Vitals 與轉換成效要在正式上線後持續量測，不能用一次 Gate 宣稱排名或引用保證。
- 正式公開站已載入帳號入口 bootstrap；候選來源現只放入核准的 staging 純導向連結，正式 live 尚未發布。候補仍是安全停用狀態，也沒有正式 receiver。
- `https://app.dealalliancehub.com/register`、`/login` 已以去識別 staging lifecycle 驗證為 HTTPS `200`、`no-store`／`noindex` 的私有頁；外部頁面沒有帳密表單，完整註冊／驗證／取消／重設／登入／登出與權益流程只由帶 Secret 的假資料驗收器執行。
- 因此候選官網 CTA 可供使用者驗收，但 **正式 live 尚未啟用**。staging PASS 只代表功能驗收已就緒，不代表已對外開放註冊；仍須完成使用者驗收、正式帳號流程與 release security Gate。

## 官網已先完成的安全入口元件

- `assets/site-config.js` 使用獨立的 `accountPortalRegisterUrl`、`accountPortalLoginUrl`、允許的 Origin 與啟用模式；候選來源只允許 `https://app.dealalliancehub.com` 的精確 `/register`、`/login` 路徑。
- 只有兩個連結同時滿足「啟用」、「HTTPS（或同源）」、「Origin 在白名單」及乾淨的精確路徑（`/register`、`/login`）時，網站導覽才會插入「建立帳號」與「登入我的工具」兩個連結；query、fragment、URL 內帳密與其他路徑一律拒絕。
- 此元件只做一般頁面導向；不含帳密欄位、`fetch`、token、cookie、session 或任何帳號資料寫入。
- 未取得主窗口的核准網址時，按鈕不會顯示。這是刻意的 fail-closed，不是少做註冊功能。

## 後台接線前，主窗口必須提供

1. **正式註冊 URL**：必須是核准的精確 HTTPS URL：`https://app.dealalliancehub.com/register`；不可含尾端斜線以外的 path、query、fragment、URL 內帳密或 token。未取得實際 readback 前，不能寫入公開 CTA。
2. **正式登入 URL**：必須是核准的精確 HTTPS URL：`https://app.dealalliancehub.com/login`；忘記密碼與驗證信失敗等敏感流程由後台自行處理，不經公開官網帶 token 導向。
3. **註冊欄位契約**：最小必要欄位、欄位驗證、email 驗證、密碼規則、錯誤訊息與同意版本。
4. **信任邊界**：官網只做 HTTPS 導向或後台核准的跨來源流程；密碼、session、token、學生 ID、訂單與授權不得經由公開官網保存或回傳。
5. **公開資料與隱私核准**：註冊目的、保存期限、刪除／更正管道、客服入口與正式隱私政策版本。
6. **驗收測資**：只提供去識別化測試帳號／測試 channel；不得把真實客戶或學生資料交給官網驗收。

## 官網端完成 Gate

- [x] 候選版導覽與首頁有清楚的「註冊／登入」入口，文案不把候補頁冒充註冊；正式 live 尚未發布。
- [x] CTA 只連到主窗口核准且 HTTPS 可讀取的後台 URL。
- [x] `/register`、`/login` 均為 HTTPS GET `200`，具 `Cache-Control: no-store`、`X-Robots-Tag: noindex` 與後台私有頁所需的安全標頭。
- [x] 未設定或端點失效時，官網 fail-closed，不顯示假的註冊成功、不送出密碼、不保存帳號資料。
- [x] 註冊、登入、錯誤、取消、返回官網與完成導向均已由後台以假帳號完整走過；公開官網只做連結。
- [x] 公開頁不把 `/app/`、`/admin/`、`/students/` 或帳號內容加入 sitemap；私有頁由後台提供 noindex、no-store 與權限控制。
- [ ] 受控發布後重新跑正式公開 GET-only Gate；註冊流程的去識別 E2E readback 已由後台線完成。

## 明確不在本聊天室執行

- 不猜測或創造註冊 API／後台 URL。
- 不接真實帳號、密碼、Email、付款、學生資料或授權資料。
- 不修改後台、Cloudflare、Worker、D1、secrets、付款或 `l2_current_truth.json`。
- 不把「有一個按鈕」稱為註冊完成；必須有後台端到端 readback 才能進 `RELEASE_READY`。

## 回滾

若後台端點失效、不是 `200`、私有標頭缺失、跨來源設定錯誤或註冊 readback 不一致，立即撤回官網註冊／登入 CTA，保留公開內容與候補頁的安全停用狀態；再重跑公開路由、metadata、安全標頭與假資料 Gate。
